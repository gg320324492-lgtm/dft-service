# 2026-09-06 工作日志

## 内部振动子空间修正批（internal_subspace_revision，纯离线，2026-09-06 凌晨）

用户下达曲率审计后续批：修正内模提取、复用已存 Hessian 重分析、旧扫描审计、补测清单（仅清单不执行）。**未启动任何量化计算。**

- **方法修正**（curvature_audit_candidates.py 的 projected_modes 重写 + curvature_audit_lib 扩展）：放弃"对 P·H_mw·P 全谱取最低值"（内模全正时必选中外模零值——上一批 c06_plus 解析探针/c14_plus 差分探针失效的确切原因）；改为 V(3N×6, 质量加权 TR 正交基) + QR 正交补 U(3N×12)，H_int = UᵀH_mwU 特征分解，特征向量经 U 映回 18 维，再按 x = M^-1/2·v·q 转笛卡尔。**不得用删除小本征值代替子空间约化**（真实软模也近零）。
- **单元验证（UNIT_TEST PASS）**：不变量 UᵀU≈I、VᵀU≈0、12 内模、VVᵀ+UUᵀ=I；四案例（全正内模不选外模零值/单负内模识别/近零软模不误删/旋转不变）全过；**PySCF harmonic_analysis 交叉核对残差 3.9×10⁻⁶ cm⁻¹**（度量一致构造下）。首版交叉核对失败的根因又是度量混用——把质量加权 Hessian 直接当笛卡尔喂给 PySCF（O 主导模被 √(1/16) 缩放）；修正为 H_cart = M^{1/2}·H_mw·M^{1/2} 后精确一致。
- **PySCF 接口核对**：harmonic_analysis 的 norm_mode 已是笛卡尔位移形式（M^-1/2·raw_mode），用原始特征向量显式转换不会重复加权。
- **两候选修正后 12 内模谱（关键结果）**：
  - c14_plus：解析 [−2.296e-4(−77.9 cm⁻¹), +1.05e-4, +2.05e-4, +3.06e-4, +7.8e-4, +2.2e-3, 2.35e-2, 6.7e-2, 6.8e-2, 0.10, 0.567, 0.598]，**1 个负内模（鞍点证据）**；FD [+8.364e-5(+47.0), +2.0e-4, ...] **无负内模（极小值证据）**。
  - c06_plus：解析 [+2.238e-5(+24.3), +6.9e-5, +2.3e-4, ...] **无负内模（极小值证据）**；FD [**−4.443e-5(−34.3)**, +3.98e-5, +5.79e-5, ...] **1 个负内模（鞍点证据）**。
  - 全部 12 内模外模重叠 = 0；解析↔差分内模重叠矩阵 12×12 已存。
  - **两种构造互相矛盾且在两候选间恰好互换**——与平台解析 Hessian 层误差（7.5e-4–2e-3）≥ 争议信号（4.4e-5–2.3e-4）的结论一致：软模符号不可分辨。
- **旧扫描审计**：c14_plus analytic_lowest 保留（外模重叠 0，与修正后内模重叠 1.0 可复用）；c14_plus fd_lowest 撤回（外模重叠 1 = 纯 TR 方向）；c06_plus analytic_lowest 撤回（外模重叠 1）；c06_plus fd_lowest 保留（与修正后内模重叠 1.0 可复用）。
- **误差阈值核查**：代码中 DE_E_FLOOR=1e-9 为假设值非实测；同几何重复误差=0（确定性）；**6.6e-8 Eh 为间接上界**（|k_H−k_E|·q² 推得，捆绑能量层非平滑+Hessian 层误差+截断），非同几何重复测量；统一可分辨性标记 = |k_E| > 3δE/q²，用间接上界 2.2e-8 时 0.005 Å 点不可分辨。max|grad| 应为 max(abs(g))，但完整梯度向量未持久化（stepC 仅存标量）→ **注明缺失**。撤回"全部是正曲率"类概括。
- **最小补测计划（仅清单）**：c14_plus 解析负模方向 + c06_plus 差分负模方向，各 3 幅度（0.002/0.005/0.01 Å 向量长度）× 2 符号 = 各 6 次 SCF+梯度，合计 12 次（level 6/严格 SCF，~5 分钟量级）。目的：小幅度下观察争议内模曲率稳定区间；**仅在平台解析 Hessian 修复或更高层验证可用时符号判定才有意义**。
- 交付：`results/01_water_matrices/pure_water_o3_h2o/internal_subspace_revision/{subspace_report.md, subspace_summary.json}`；原始 `run_artifacts/.../internal_subspace_revision/internal_subspace_analysis.json`。新脚本：subspace_revision_analyze.py、subspace_revision_report.py；curvature_audit_candidates.py 的 projected_modes 已修正。
- **判定**：两候选维持**待定**（几何收敛未完成 + 软模符号在干净内模子空间中仍不可分辨）。本批停止；后续等验收后决定针对性补测（12 SCF+梯度）、终点优化或后端根因排查。
- 教训：(1) numpy eigh 特征向量按列取；"最低模"选择必须来自子空间约化而非全谱截断；(2) 跨度量比较（质量加权 vs 笛卡尔）是本项目曲率工具三类错误的共同根源——任何 d·g/ΔE 与 dᵀHd 的对比前先确认同一笛卡尔方向；(3) 离线重分析前先持久化完整矩阵/向量/设置，本批因此零量化计算完成全部重分析。

## 定向补测批（directional_confirmation，2026-09-06 凌晨）

用户下达两个有效争议内模的定向补测批（双网格 level 6/7 配对，28 记录）。产出：`results/01_water_matrices/pure_water_o3_h2o/directional_confirmation/{directional_report.md, directional_summary.json}`；28 个 point_*.json（含完整梯度、DFT/D2 分量、收敛、网格点数）。**未重算完整 Hessian、未启动长优化/高层级单点。**

- **中心梯度证据补齐**：c14_plus max(abs(g))=1.384e-5（L6）/1.784e-5（L7）；c06_plus 1.467e-5（L6）/1.877e-5（L7）——全部 >1e-5 目标，几何收敛未完成确认；完整梯度向量已持久化（此前缺失项补齐）。28/28 全部 SCF 收敛。
- **关键数据**：c14_plus（解析负模方向）k_E 与 k_g 两网格三幅度**全部为正**（k_E +1.1e-4→+3.4e-4；k_g +2.6e-4→+5.0e-4），外推 k0(E)≈+1.0~1.3e-4、k0(g)≈+2.5~2.8e-4，网格稳定（Δgrid(k_E)~3e-5）→ **解析 Hessian 的 −2.4e-4 被能量/梯度层直接测量否定**。c06_plus（差分负模方向）**网格间符号翻转**：L6 正（+2.9e-4→+5.0e-4）、L7 零负（−5.0e-5/+8.0e-7/+1.8e-4），Δgrid(k_E)~3.2–3.4e-4（64–117%）→ 待定无疑义。斜率两估计之差 |s_E−s_g|~2.5e-5 = 梯度场跨几何非平滑性直接观测。
- **判定**：两候选均**待定**（c14_plus：E/g 符号一致为正但幅值差 >50%、grid-stable=True；c06_plus：网格依赖 117%、E/g 不一致）。建议均为"最小导数诊断优先"。
- **按要求撤回**：|k_H−k_E|·q² 推"能量误差上界"再裁决可分辨性的做法；"同几何重复相同=无离散化误差"；"已证明不可分辨"措辞（逐项列出数据不能支持的结论）。
- 新脚本：directional_confirmation.py（--report-only）、directional_confirmation_report.py。教训：shell 重定向不创建缺失目录（后台启动先 mkdir）；bash -c 内嵌中文模式的 python 补丁会乱码——用 ASCII 锚点。
- 本批停止。两候选在几何与完整内部频率验收前继续作为候选保留；随后才恢复水相结果更新与反应路径研究。

## 位移单位修复批（JOB-2026-0906-002，2026-09-06）

用户下达位移单位修复与重分析批（纯离线）。交付：`results/.../unit_consistency_revision/{unit_consistency_report.md, unit_consistency_summary.json}`；回归测试 `protocols/test_unit_consistency.py`（ALL PASS）；根因记录 `notes/unit_defect_root_cause.md`；任务记录 `jobs/JOB-2026-0906-002_unit_consistency_revision.md`。

- **缺陷确认**：q_for_atom_disp 返回 Bohr 约定幅度，但 directional_confirmation.py 与 grid_response_diagnostic.py 把 q·d 加到 Å 坐标 → 实际幅度 1.8897×标称（38 点逐一实测确认：0.002→0.003779 Å 等）；k_E 低估 ×0.28、k_g/s_E 低估 ×0.529。
- **受影响**：directional_confirmation 28 点 + grid_response_diagnostic 10 点。**不受影响**：curvature_audit 探针（Bohr 坐标自洽）、stepD 模式扫描（Bohr）、H₂O 三层定位（Bohr）、单元测试、全部频谱/子空间分析。
- **单位修正后关键结论**：(1) 全部能量层与梯度层曲率沿两个争议方向均为正或 ≈0——**无负曲率证据存活**（c14_plus k_E +3.1→+9.5×10⁻⁵；c06_plus 小幅度 ≈−1.3×10⁻⁵≈0）；(2) c14_plus 的 k_E–k_g 层间差异由缺失网格响应项解释——开启 grid_response 后 k_g(True,corr) ≈ k_E(corr)（+4.4 vs +3.9×10⁻⁵，13% 内一致），旧"过冲"结论撤回；(3) c06_plus 小幅度处 E/G 本就一致（旧"假设未获支持"结论按新分析重新表述）；(4) 剩余差异随幅度呈 O(q²) 非谐增长（有限证据，两步长）。
- **判定更新**：两争议方向的负曲率争议**关闭**（该方向曲率为正或 ≈0）；但两候选整体仍**待定**（几何收敛 <1e-5 未达标 + 其余 11 内模未核查 + 平台解析 Hessian 层未修复）。
- 修复：两脚本位移行加 BOHR_A 因子；regression test 含修复前缺陷复现项。
- **下一批**：scanner 调用验证（统一梯度工厂 grid_response=True + D2 一次）→ 有步数上限的终点重优化（验收梯度 = grid_response=True）→ 新终点完整内部频率验收。

## 三阶段批（JOB-2026-0906-003：验收补正→scanner 验证→60 步终点重优化）

- **阶段 A 通过**（先前会话完成，本批复核）：权威报告 `phaseA_unit_revision_report.md`（网格 L6/L7 列+单位、c06_plus 撤回"≈0/争议关闭"、准确记录 k_E=-1.4065e-5 与 k_g(True)=-1.1339e-5 均为负、待判断）；回归 28/0（含负曲率符号保持、四次项 O(q²) 公式验证、去 BOHR_A 后必失败项）；audit.py 满足 q 全精度/SCF 字段缺失=False/覆盖率/missing 旧值不算通过。旧 002 报告头部已补 003 取代关系。
- **阶段 B 通过（overall_pass=True，机器精度）**：`protocols/grad_factory.py` 统一梯度入口（make_mf_d2_gr → nuc_grad_method 恒 grid_response=True；D2 经 attach_d2 恰一次）。A→B→A 同 scanner 验证 c14_plus L7 中心：D2 计一次（d2_e_resid≈8e-14，d2_g_resid≈1.5e-10，|D2|=1.7e-4）、scanner=独立DFT(True)+D2（dE~1e-13）、grid_response 恒 True 且实际活跃（vs False 差 6.8e-5）、可复现无残留（repro_g=2.2e-10）。产物 `run_artifacts/.../phaseB_scanner_verify/scanner_verify_c14_plus_L7_center.json`（水二聚体 smoke 同样通过）。
- **B 阶段排障记录**：(1) GradScanner 不回写 scan_mf 密度→同密度基准不可行，改用全新 mf 独立基准；(2) 独立基准曾漏设 grid_response=True（g_dft 变 DFT_False），致 d2_g_resid≈|g_D2| 假象——scanner 实际无任何错误；(3) make_mf_d2_gr 曾错误重赋 _d2_parent_cls=RKS_D2（实例属性查类不存在回退），致"独立基准"能量已含 D2——修复为继承原实例属性。
- **pyberny 0.7.0 收敛参数（已核实，与 PySCF 文档示例不同）**：`gradientmax/gradientrms/stepmax/steprms`（BernyParams，原子单位，全 AND，无能量判据），默认 0.45e-3/0.15e-3/1.8e-3/1.2e-3；经 **kwargs 直接传入；maxsteps 独立传。Phase C 设 gradientmax=gradientrms=1e-5，步长默认，maxsteps=60。
- **阶段 C 运行中**：`protocols/phaseC_reoptimize.py`（berny_solver.kernel + make_mf_d2_gr，callback 逐步坐标/E/g/SCF 状态/D2 分量，非有限值即停，独立新建对象复核 max|g|≤1e-5，端点 RMSD 去重检查）。step1 能量与 Phase B scanner 完全一致。

### 阶段 C 结果（attempt 2，正式）
- attempt 1（gradientmax=1e-5）暴露判据表征差异：优化器 6/13 步"收敛"但 Cartesian max|g|=3.13e-5/1.38e-5 > 1e-5——pyberny 判据作用在**内坐标梯度** dot(B_inv.T,g)（berny.py L294）。存档取代（phaseC_summary_attempt1.json）。
- attempt 2（内坐标阈值收紧 1e-6，无放宽，原始起点重跑）：**c14_plus 46 步收敛、独立复核 max|g|=2.641e-6 ✓；c06_plus 25 步、2.142e-6 ✓**（均 ≤1e-5，未触 60 步上限）。端点能量 c14 −301.874837947 / c06 −301.874093449；ΔE −2.64e-6 / −1.04e-6 Eh；端点 D2=解析值（恰一次）；端点 RMSD=1.594 Å（间距 2.893 vs 3.663 Å）不同构型，均保留。
- 交付：`results/.../phaseC_reoptimization/JOB-2026-0906-003_three_phase_report.md` + `JOB-2026-0906-003_three_phase_summary.json`；任务记录 `jobs/JOB-2026-0906-003_three_phase_batch.md`。
- 状态：两候选 = **已收敛驻点候选**（非最终极小值）。下一批：新端点完整内部频率 + 软模验收 → 水相/高层级/反应路径。本批停止。

## 端点频率验收批（JOB-2026-0906-004，2026-09-06）

用户下达"新端点完整内部频率与软模可靠性验收批"（不重优化/不追负模/不水相）。交付：`results/.../endpoint_frequency/{endpoint_frequency_report.md, endpoint_frequency_summary.json}`；任务记录 `jobs/JOB-2026-0906-004_endpoint_frequency_batch.md`。

- **端点固定**：attempt2 正式端点 + 索引核实（臭氧中心 O=0、端氧[1,2]、水H[4,5]）；中心检查 c14 max|g|=2.641e-6、c06 2.142e-6 通过，完整 6×3 梯度已存。
- **003 批补正 4 项**：逐步完整梯度缺项（仅 max/RMS，不虚构）；撤回"正式配准去重"（rmsd_dedup 仅质心平移）；方法命名统一（ωB97X-D 非经验色散 DFT 部分 xc='wb97xd' + 项目经验色散全导数，不是独立 ωB97X 泛函）；attempt2 属追加尝试（6/46、13/25 步），不再追加。
- **正式去重**：Kabsch（O₃ 框架锚定）+ 置换{1↔2,4↔5}，c14↔c06 RMSD=3.745 Å → 不同构型均保留；各端点 vs 起点 0.07 Å。
- **144 点双步长 Hessian**（18 方向 × ± × 0.002/0.004 Bohr）：全部 SCF 收敛、grid_response=True、坐标核验 <1e-10 Bohr；反对称残差 2.2–9.4e-6；未对称化 Hessian 已存。
- **频率结果**：**两候选 12/12 内部模式在两步长下全部为正**（c14 最低 42.1 cm⁻¹、c06 32.5 cm⁻¹）；与本机 PySCF harmonic_analysis 交叉核对 h=0.002 差 ~1e-11 cm⁻¹（机器精度）；两步长重叠 ≥0.992，max|Δω| 0.08/0.77 cm⁻¹。
- **关键 bug 修复（脚本级）**：h_mw 质量加权时对已是 (p,x,q,y) 布局的数组误套 PySCF 的 transpose(0,2,1,3) → 双重转置破坏合同变换符号保持（假 6 负模）。诊断方法：Sylvester 惯性定律（Hsym PSD 但 h_mw 出现 −0.584 → 必为索引错位）。修复后与 PySCF 机器精度一致。
- **软模核查**（32 点 L7/L8 + L8 中心）：两候选最低模曲率 L7/L8 全正、斜率校验 ≤1e-9、与 Hessian 投影一致；c06 曲率网格鲁棒（<1%）；**c14 最低模（42.1 cm⁻¹）量级 L8 敏感（~3×）且 c14 的 L8 中心 max|g|=1.475e-5 略超 1e-5（已记录，L7 验收不受影响）**。
- **判定**：两候选 = 在本批方法与数值检查范围内支持的局部极小值（非全局最低）。无鞍点/无待定。下一步（用户决定）：水合结构筛选、可选 c14 最低模网格收敛专项、恢复水相/高层级/反应路径。本批停止。

## grid_response 配对诊断批（JOB-2026-0906-001，2026-09-06 凌晨）

用户下达 grid_response 诊断批（10 几何点 × 双梯度开关，level 7）。交付：`results/.../grid_response_diagnostic/{grid_response_report.md, grid_response_summary.json}`；任务记录 `jobs/JOB-2026-0906-001_grid_response_diagnostic.md`。

- **Q1 历史未开启**：Gradients.grid_response 默认 False（rks.py L745，无配置覆盖）；开启路径 = get_vxc_full_response + extra_force(exc1_grid)（L50/L772）。D2 包装不遮蔽实例属性，D2 与网格无关。
- **Q2 分候选结论**：c14_plus——开启后 gap 从 +1.5e-4 缩至 −6e-5（响应贡献 ≈ −2.1e-4，过冲），**部分解释**；c06_plus——gap_F≈gap_T≈3e-5，**假设未获支持**。响应项形态 = 缓慢变化偏移场（gT−gF ≈ 2.5–3.0e-5 恒定）。
- **Q3**：开启后中心 max|g| 2.20/2.43e-5 仍 >1e-5（略增）——几何收敛与网格响应是独立问题。
- **Q4 scanner 验证**：待下批（本批仅验证实例赋值生效）；需确认 scanner.copy() 后开关存活。
- **Q5**：两候选均建议最小导数诊断优先（剩余 gap ~6e-5/~3e-5；解析 Hessian 层已知平台误差 7.5e-4–2e-3 可覆盖之）。
- **撤回**（按要求）：|k_H−k_E|·q² 推能量上界再裁决可分辨性的做法；"同几何重复=无离散化误差"；"已证明不可分辨"措辞。能量不变性对照通过（开关不改 SCF 能量）。
- 判定：两候选维持**待定**；下一步=最小导数诊断（处理剩余 gap），暂不做终点优化。本批停止。

## 频率交叉核对修订 + c14 L8 驻点核查批（JOB-2026-0906-005，2026-09-06 深夜）

- **阶段 A 完成**：step3 修复（每步长自身 Hsym 交叉核对 + 同矩阵断言 + 回归 12/12）；撤回 h=0.004"舍入"解释（实为误传 h=0.002 矩阵，ratio=1.000000）；同矩阵核对 h=0.004 = 4.5e-11/1.1e-10 cm⁻¹；矩阵比较三分离（Cartesian/同 U 内部/模式匹配）；软模实际 16 点（上限 32 非执行量）；c14 标"L7 证据支持、网格敏感性待完成"。

### 阶段 B/C 结果（同批完成）
- **阶段 B 通过**：c14_plus 一次 L8 优化（从 L7 attempt2 端点，仅网格改 L8，其余参数不变）45/60 步收敛；独立新建 L8 对象复核 max|g|=2.541e-6 ✓（dE 复核 −4.6e-13）；逐步完整 6×3 梯度已存（004 缺项闭合）；正式配准 L7↔L8 端点 RMSD=0.0204 Å（结构保持）、vs c06 3.743 Å。
- **阶段 C 通过**：新 L8 端点 72 点双步长 Hessian 全部接受；**12/12 内部模式在 L8 双步长下全正**（最低 38.2/38.5 cm⁻¹）；同矩阵 PySCF 交叉核对 ~1e-11 cm⁻¹；最低模 4 点核查 k_E/k_g/Hproj = 6.02/6.07/5.97×10⁻⁵ 全正一致。
- **关键澄清（006 批修订归因）**：L8 驻点处 L7/L8 曲率比降至 ~1.18（7.089/6.018）。~~004 批 4.03 的比值主要来自非驻点位置比较（L8 梯度未消），非纯网格敏感性~~ → 重新优化后各自最低方向曲率差异减小；重优化同时改变几何与模式方向，两者贡献尚未单独分解，不单独指认非驻点为主因。
- **判定**：c14_plus = "在 L7/L8 已实施检查范围内支持的局部极小值"；c06_plus 维持 004 判定（L8 全谱未查、不在授权内）。
- 交付：`results/.../c14_l8_check/JOB-2026-0906-005_phase_report.md` + `JOB-2026-0906-005_phase_summary.json`；任务记录 `jobs/JOB-2026-0906-005_revision_l8_batch.md`。本批停止；下一批决定水合结构保留、水相更新与高层级对照。

## 气相归档 + SMD 水相验证与优化批（JOB-2026-0906-006，2026-09-07）

- **阶段 A**：气相登记表（c14 主参考=L8 端点、c06=L7 端点，来源/哈希/频率证据/判定范围）；能量不可跨网格挂接；005 归因修订（几何+模式方向同时变化、未单独分解）。
- **阶段 B 通过**：SMD 五点验证。源码级核对：e_tot = DFT + e_solvent + e_cds；CDS 为 libsolvent C 例程解析实现（非有限差分）；**D2 同密度计一次达机器精度**（E~1e-14、g~3e-15）；grid_response 在 SMD 梯度中活跃（差 2.76e-5）；斜率=投影梯度。
- **包装修复**：`d2_full.undo_solvent` 单层剥离留 RKS_D2（闭包引用 SMD 父类）→ 重入溶剂机制断言失败；**若不失败则 SMD 梯度中 D2 双重计入**。修复 = 剥全部项目包装类至干净 RKS。记录 `notes/smd_d2_undo_solvent_fix.md`。
- **阶段 C**：两候选各一次 ≤60 步 SMD(water)/L8 优化，**均未达 max|g|≤1e-5**：c14 触 60 步上限（max|g|=2.39e-3，最低曾 8.3e-5 后振荡回升）；c06 优化器 40 步提前停止（内坐标满足）但 Cartesian 复核 2.58e-5。判"未收敛"、保留现场、不追加。能量弛豫 c14 −1.51e-3 / c06 −6.17e-4 Eh。氢键形成（H···O 2.28/2.11 Å），无解离。去重 RMSD 3.91 Å（两来源保留）。
- 交付：`results/.../smd_restart/{smd_restart_report.md, smd_restart_summary.json}`；任务记录 `jobs/JOB-2026-0906-006_smd_restart_batch.md`。
- **下一批（用户决定）**：更长步数上限 / 更近水相起点 / 阈值讨论 → 水相频率验收 → 反应路径。
