# 2026-09-05 工作日志

## Part 2（JOB-2026-0905-009）完成：O₃·H₂O 局部水合构型（纯水）

过程记录（geomeTRIC 收敛键、undo_solvent MRO、虚频符号、ZPE 公式、端点注册等坑）见 `2026-09-04.md` 末三节。最终结果：

- **Stage 1**：16/16 收敛（max|grad| < 5e-5）。
- **Stage 2 + 2b**：14 试探单结构中仅 c05 为真极小值，其余为鞍点（1–5 个虚频）；对 7 个单虚频鞍点沿虚频 ±0.25 Å 模式跟进（14/14 成功）→ 端点 8 个新真极小值。**最终：3 kcal/mol 窗口内 9 个不同真极小值**，全局最低 **c14_plus = −301.87483581 Ha**（c12± 与之差 <0.0001 kcal/mol；c06_plus +0.47）。极平坦弱关联 PES 多浅阱图像。
- **Stage 3**（SMD）：9 → 5 个不同 SMD 极小值（c05、c14_plus、c09_plus、c11_minus、c06_plus；4 个在 SMD 中坍缩合并）。dE(SMD−gas) ≈ −10.77~−10.99 kcal/mol（近均匀溶剂化稳定化）。仅作局部水合结构趋势，非水溶液结合常数。
- **Stage 4**：E_int(非CP) ≈ −2.05、**E_int(CP) ≈ −1.53**、BSSE ≈ 0.5 kcal/mol；ZPE 贡献 +0.96~+1.19；**ΔG_assoc(1 atm) ≈ +2.99~+5.05、ΔG_assoc(1 M) ≈ +1.10~+3.16 kcal/mol**（1:1 弱缔合在标准态下endo，符合平动熵惩罚预期）。ghost-D2 排除审计通过（d2 差 = 0.0）。
- **Stage 5**：CCSD(T)/aug-cc-pVTZ，两个最低真极小值 c14_plus/c12_plus：**E_int = −2.810 kcal/mol**（两者一致），T1 = 0.064 > 0.02（O₃ 多参考本性，如实标注，不降级）。
- **交付物**：`results/01_water_matrices/pure_water_o3_h2o/{conformer_summary.json, conformer_report.md}`（推荐 c14_plus 为下一阶段初始结构）。
- 新脚本：`run_stage2b_follow_modes.py`（虚频模式跟进）、`register_follow_endpoints.py`（端点注册）、`run_downstream.sh`（流水线驱动）。
- 用户问 GPU 加速：机器有 RTX 5090 32GB（WSL 可见、闲置）。结论：当前 6 原子/136 bf 体系收益有限（2–5×）、CCSD(T) 无 GPU 路径、attach_d2+SMD 已审计路径不可移植；近期更划算的是把数值 Hessian 的 36 位移梯度并行化（纯 CPU）；将来大水簇用 GPU ML 势预筛 + CPU 审计路径精算。
- **下一步（用户既定方向）**：选一条纯水中臭氧相关反应作首个机理案例，再逐步加 HCO₃⁻、Cl⁻、铵态氮等水体基质。

## JOB-009 结果复核修订批（2026-09-05 下午，acceptance_revision）

用户下达系统复核指令（暂停反应路径研究），8 项错误全部查实并修复，产出 `results/01_water_matrices/pure_water_o3_h2o/acceptance_revision/{revision_report.md, revision_summary.json}`（取代 v1 报告，旧版保留）。原始审计输出在 `run_artifacts/01_pure_water_o3_h2o/acceptance_revision/`。

- **E1 氢键判据反置（最关键）**：`hbond_topology` 用 O_w→H 而非 H→O_w 向量套 ≥120° 判据 → 线性氢键反被排斥。实测 c02 有 d(H···O)=2.286 Å、角 179.9° 的氢键被判 "none"；c01/c03/c04/c06/c08 均有 148–167° 真氢键漏判。
- **E2 配准中心不一致 + 缺水氢置换**：best_rmsd 旋转用 O3 框架、平移用全分子质心；改为 O3 质心锚定 + O3 末端×水双氢 4 种换号。合成案例 12 个 RMSD≈1e-16 全过（revision_test_1_rmsd_topology.json）。
- **重大修订结论**：修正去重后 **v1 "9 个不同极小值" 坍缩为 2 个待核验候选**（c14_plus = 已搜索候选中的最低能结构 −301.87483581 Ha；c06_plus +0.47）；c12±/c05/c11±/c09± 是 c14_plus 的等价重复（修订 RMSD 0.014–0.083 Å）。SMD 侧同样 2 个候选（c05、c06_plus）。v1 的成对 RMSD 被旧配准逻辑系统性虚高。
- **E6 能量定义混用**：v1 Stage-4 "E_int(非CP)" 实为结合能、"E_int(CP)" 实为相互作用能。统一定义重算（revision_unified_energies.json）：c14_plus E_int(noCP)=−2.051/CP=−1.531/BSSE=−0.520/E_def=0.002/E_bind=−2.049(noCP)、−1.529(CP)；c06_plus E_bind(CP)=−1.338。BSSE 符号约定= E_int(noCP)−E_int(CP)≤0 已明示；ΔG 不含 CP 且与 E_bind(noCP) 配对。
- **E7 CCSD(T) 诊断**：0.0643 重标为 max_abs_t1；标准 T1（pyscf get_t1_diagnostic=sqrt(|t1|²/N_corr_elec)）复合物仅 **0.0189/0.0191（低于 0.02 阈值！）**、O₃ 片段 0.0217——旧 single_reference_ok=False 方向都错了，判定已撤回；D1≈0.069–0.071 仍提示 O₃ 多参考特征，留后续证据。34 相关电子、frozen=0、RHF/CCSD 全收敛；t1 存 .npy，t2（~1GB）未持久化已列缺失。
- **E-频率可靠性（新发现）**：解析 vs 独立差分 Hessian 相对差 ~2.7e-3 且不随步长收缩；**最低软模符号在构造方式间翻转**（c14: 1vs0；c14_plus/c12_plus: 0vs1）。网格 level-6+SCF1e-12 收敛检查：c12_plus 最低模 +48.8→−29.8 cm⁻¹（移位 79！）→ **c12_plus 实为鞍点（解析 L5 误判）**；c14_plus 全构造 n_imag=0 稳定极小值；**c14 UNSTABLE → 待定**。负模一律不删不归噪声。
- **E8 方法命名**：基线生成器统一改为"ωB97X-D 的不含经验色散部分用于优化/频率，另加经验色散单点校正"，保留 xc='wb97xd'/Libxc 条目名，不得等同独立 ωB97X 泛函；baseline_summary/acceptance/calculation_records 已重新生成。
- 新脚本：revision_test_1_rmsd_topology.py、revision_step2_rededup.py、revision_step3_hessian_check.py、revision_step3b_grid_check.py、revision_step4_ccsdt_t1.py、revision_step5_energies.py、revision_step6_report.py。
- **本批未启动**任何反应/过渡态/离子/界面计算。下一步：本批验收通过后决定保留哪些水合结构、是否补高层级对照，再另批选首条臭氧反应路径。

## 最终极小值验收批（final_minima_validation，2026-09-05 晚）

用户下达 c14_plus/c06_plus 最终几何/频率验收批（完成后停止）。产出：`results/01_water_matrices/pure_water_o3_h2o/final_minima_validation/{validation_report.md, validation_summary.json}`；原始输出在 `run_artifacts/.../final_minima_validation/`；新模型 `models/01_water_matrices/pure_water_o3_h2o/validated/{c14_plus,c06_plus}_validated_pending.xyz`。

- **报告状态更正**：revision_report 加勘误——撤回"c14_plus 全构造零虚频"与"FD@0.005 属剪枝噪声"；c12_plus 改为"存在负曲率证据"，与 c14_plus 关系仅为几何聚类。
- **平台级根因定位（治理流程，平台未改）**：`notes/platform_issue_wb97xd_hessian.md`——最小复现（H₂O/def2-TZVP 纯 PySCF）：解析 Hessian vs 解析梯度差分，pbe 2.8e-5→4.3e-4（h² ✓）、b3lyp 5.0e-5→4.5e-4（✓）、**wb97xd 1.9e-3→1.8e-3（不收缩）**——wb97xd 解析二阶导与解析梯度不自洽 ~2e-3。剪枝/筛分归因被配对对照排除（四种设置跳变相同；small_rho_cutoff 默认=0）。
- **统一精度重优化**：level 6 + conv_tol 1e-12 + conv_tol_grad 1e-9、gmax 目标 1e-5、maxsteps=400——两候选均**未达标**（新鲜复核 1.38e-5 / 1.47e-5；平势面爬行至 SCF 噪声底，信任半径塌缩至 1e-4 Å；不放宽标准，保留证据标待定）。旧→新几何 RMSD 0.030/0.070 Å。
- **软模独立验证（步骤 D）符号冲突表**：c14_plus 解析 −87.4 / FD@0.02,0.03,0.05 −15.7,−15.0,−17.4 / **能量扫描 +68.4→+129.9→+249.8（强非谐正曲率，随幅度增大）**；c06_plus 解析 −28.8 / FD −35.5,−72.9,−136.2 / 扫描 +19.0→+34.1→+65.3。扫描（SCF 能量平滑 1e-10，绕开解析二阶导）显示沿 ±模能量上升、0.2 Å 内无更低结构——**谐振符号低于当前平台可分辨极限**。
- **最终判定：两个候选均"待定"**（非不通过——负曲率未被扫描证实；非通过——梯度未达标+符号冲突）。身份：新终点 RMSD 3.74 Å、ΔE 0.466 kcal/mol、拓扑不同 → **两个独立候选不合并**；c14_plus 为已搜索候选中能量最低（−301.87483610 Ha）。RRHO 自由能保留为历史值。未执行负模跟进（扫描显示无可跟进下降方向，避免追数值伪影）。旧→新几何变化 <0.1 Å 非实质变化，CCSD(T) 未重跑。
- 新脚本：validation_stepB_settings_audit.py、validation_stepB2_platform_repro.py（支持 --only/--out）、validation_stepC_reopt.py、validation_stepD_softmode.py、validation_stepEF_report.py。
- 教训：(1) 混合编码环境下经 bash -c 内嵌 python 长代码极易引号崩坏——一律写脚本文件执行；(2) 配对对照设计要先分离"真实曲率信号"与"噪声"（±位移跳变含真实曲率，同几何跨设置比较才测噪声）；(3) exec() 复用脚本改循环会破坏 __file__ 路径解析导致 JSON 落盘丢失——改用 --only 参数正规重跑。

## 曲率审计批（curvature_audit，2026-09-05 深夜）——复核验证工具本身

用户下达工具复核批（判定前先统一振动子空间/模式方向/曲率定义）。产出：`results/01_water_matrices/pure_water_o3_h2o/curvature_audit/{curvature_report.md, curvature_summary.json}`；原始在 `run_artifacts/.../curvature_audit/`（H2O 三层定位、最差元素探针、单元测试、两候选 Hessian 矩阵 .npy 持久化）。

- **验证脚本层 4 处缺陷（均已修复+单元回归）**：T1 能量差分分母 2q²→q²（撤回 √2 表述）；T2 最大原子位移改用每原子位移向量长度（旧分量约定差 2 倍）；T3 **三层度量不一致**（k_H 质量加权度量 vs k_E/k_g 笛卡尔度量，O/H √(mᵢmⱼ) 差 16 倍 → 旧探针的 3.4e-3 vs 2.3e-4 "~15 倍差"是度量伪影）；T4 未投影 TR 子空间（原始 3N 谱 ±10 cm⁻¹ 噪声模污染"最低模"）+ 特征向量按行切片 bug（v[:12][0] 取到模混合物，第一轮探针数据作废重测）。
- **单元测试（UNIT_TEST PASS）**：二次势已知 λ=−1.234e-4 三种曲率精确往返；旧分母 bug 复现（k/2）；正符号往返；向量长度约定；12 内模/6 外模投影正确。
- **平台问题层级定位（更新 notes/platform_issue_wb97xd_hessian.md）**：状态="疑似后端导数不一致（Hessian 层），根因待定位"。证据：H₂O 最差元素方向三层比较——能量层与梯度层互洽（≤5e-5@0.005 Å）且 h→0 外推同收敛 ~0.21745，解析 k_H=0.21669 恒定偏离 ~7.5e-4 → 不一致在解析二阶导层；元素特异（O–H 伸缩方向三层互洽 ~4e-4）；能量层数值噪声底实测 6.6e-8 Eh。问题分层：项目包装=排除、验证脚本=已修复回归、PySCF/Libxc=疑似（具体响应项待定位）、DFT 源码=排除。
- **两候选最终判定（均"待定"）**：投影内模子空间中两种 Hessian 构造互相矛盾——c14_plus 解析 −2.3e-4（鞍点证据，外模重叠 0.0000 干净）vs FD 无负内模（最低 +8.4e-5）；c06_plus 解析全正（+2.2e-5）vs FD −4.4e-5（鞍点证据）。能量/梯度层沿争议方向给正曲率但随幅度漂移且最小幅度贴近噪声底（能量层噪声 6.6e-8 Eh）。解析平台误差（7.5e-4–2e-3）与差分噪声（4e-4–1.6e-3）均 ≥ 争议信号（2e-5–2.4e-4）→ 符号不可分辨。
- **撤回 4 处表述**："0.2 Å 内不存在更低结构"、"不存在可跟进的下降方向"、"软模低于平台可分辨极限"（未量化版；量化后重新陈述）、"RMSD<0.1 Å 因此能量不受影响"。CCSD(T) 绑定原始几何为历史单点。
- 新脚本：curvature_audit_lib.py、curvature_audit_unit_test.py、curvature_audit_h2o_layers.py、curvature_audit_h2o_worst.py、curvature_audit_candidates.py、curvature_audit_report.py。
- 教训：(1) numpy eigh 的特征向量是列——v[:12][0] 取行是隐蔽 bug，混合模方向的外模"重叠"异常值（0.49/0.76）是识别线索；(2) 方向导数三类估计量必须同度量同方向，质量加权/笛卡尔混用时 O/H 质量差造成量级伪差；(3) 单元测试（已知曲率二次势）应先于任何真实体系三层对比运行。
