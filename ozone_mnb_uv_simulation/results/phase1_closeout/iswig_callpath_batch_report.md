# JOB-2026-0906-017：ISWIG 实际调用路径核实与固定方向对照批 · 报告

## 本批在做什么 / 已完成什么 / 还需要什么

- **在做什么**：核实当前 SMD 计算路径能否一致地使用 ISWIG；在支持条件满足时执行同几何/同方向/同表面档位的 SWIG/ISWIG 五点对照。
- **已完成**：静态源码逐行核实 + 运行时跟踪（无 SCF、项目内临时包装、安装源码未修改）；**前置检查未通过（FAIL）——按批令在求值前停止，未执行任何求值**；JOB-016 的 Hessian 表述已补正；缺口清单与后续建议已提交。
- **还需要**：SWIG/ISWIG 对照在当前安装版本**不可执行**（缺口见下）；若需恢复该路线须走平台修复审批；第一阶段收尾的可行补充 = 受限电子能流程（路线 B）。

**前置检查结果**：**FAIL（6 项关键核实中 3 项未满足）**。**实际求值数：0**（上限 5，未使用）。**完整驻点验收状态：仍未通过**（2.503×10⁻⁵ > 1e-5，未变）。

---

## 1. 实际调用路径核实（详细记录：`notes/iswig_callpath_verification_2026-09-07.md`）

运行时跟踪（`protocols/iswig_callpath_trace.py`，固化可复跑；产物 `run_artifacts/01_pure_water_o3_h2o/iswig_callpath/runtime_trace.json`）+ 源码逐行（PySCF 2.14.0）：

| # | 核实项 | 结果 | 证据 |
|---|---|---|---|
| 1 | build() 把方法传给 gen_surface？ | **否** | `smd.py:430` 无该实参；运行时：属性设 ISWIG 后 gen_surface 实际收到 "SWIG" |
| 2 | ISWIG 分支实际执行？ | **否** | 产物表面含 SWIG 专属键 `R_in_J/R_sw_J`、无 `R_J` |
| 3 | 属性被读取？ | **否**（build/梯度侧） | 全包检索仅 `smd.py:385`（_keys）与 `:403`（默认值） |
| 4 | 梯度导数与 ISWIG 一致？ | **否（接线断裂）** | `get_dF_dA(surface, method="SWIG")`；调用者 `grad/smd.py:55` 只传 surface → 恒 SWIG 分支；ISWIG 数学分支存在但仅底层直调可达（直调验证可运行） |
| 5 | 方法分支公式仅适用 SWIG？ | 分支齐备但**分发不一致** | Hessian 侧**读取对象属性**（`hessian/pcm.py` 多处 `method=pcmobj.surface_discretization_method`）→ 设属性将致能量/梯度（SWIG）与 Hessian（ISWIG）互不一致（对 SWIG 表面取 R_J → KeyError） |
| 6 | 表面数据被能量消费？ | 是（SWIG 表面） | `build()` 后 `get_F_A/get_D_S` 消费实际构建的表面 |

## 2. 门判定与处置

按批令："如不能确认 ISWIG 能量及梯度路径一致支持，停止求值，提交明确缺口，不自行修补安装库。"

**处置**：未执行第二节（5 次求值）与第三节（逐幅度对照）——前置条件不成立。**新增求值数 = 0**；无原始求值记录与对照表（无数据可产）。

**缺口清单（PySCF 平台侧，独立最小案例已具备 = 本批运行时跟踪，可复现）**：
1. `SMD.build()` → `gen_surface` 缺 `surface_discretization_method` 实参；
2. `grad/smd.py` → `get_dF_dA` 缺方法实参；
3. Hessian 侧读取属性而其余侧不读取 → 属性接线三处不一致。

**边界**：本批不修复平台（未授权）；任何平台修复须走既定流程（复现命令/根因/影响范围/最小方案 + 回归测试 + notes 登记）——本记录已提供可复现基础。

## 3. JOB-016 表述补正（已落实，可追溯）

`notes/aqueous_method_route_evaluation_2026-09-07.md` 两处：
- "解析梯度与解析 Hessian 均实现" → **hess() 入口存在 ≠ 全部贡献解析**：静电部分（nuc/qv/solver）解析；**CDS 二阶导为有限差分**（`hessian/smd.py get_cds` 源码显式警告 "Using finite difference scheme for CDS contribution"）；
- 路线 A 标注 **"当前安装版本不可执行"**（本批证据），ISWIG 仅底层直调可达、超出标准生产路径、未经授权不得采用。
- 本批不计算 Hessian（符合批令）。

## 4. 后续唯一建议（暂不执行）

**推荐：采用明确标注的"受限电子能流程"（016 评估中的路线 B）作为第一阶段收尾的补充证据路径。**
- **内容**：在气相已验收几何（c06 L7 端点、c14 L8 端点，来源已核验）上做水相 SMD 单点（能量+梯度，不优化），结果以"气相几何 + 水相单点"标签登记，仅用于**范围受限的电子能分析**（两候选水合电子能量变化的定性比较）。
- **决策价值（018 批措辞补正）**：ISWIG 路线因当前 PySCF 的 ISWIG 配置接线问题不可执行、档位追加已封存——路线 B 是**当前已核实的可行选项之一**（无需平台修复、无需新验收假设），不排除未来经审批的平台修复或其他方法验证路线；其"不能替代溶液驻点/热化学"的边界已在 016 评估中写明。
- **不推荐**：导数一致性调查与有限方法验证均以 ISWIG 可用为前提（当前不成立）；平台修复另行按流程立项（证据已记录），不与本建议捆绑。
- **执行前须总指挥批准**；本批不执行。

---

**结尾**：实际求值数 = **0**（前置 FAIL，5 次预算未动用）；前置检查结果 = **ISWIG 路径不可达 + 三处接线不一致（运行时实证）**；完整驻点验收 = **仍未通过**（2.503×10⁻⁵ > 1e-5）。本批停止：不追加档位、不算 Hessian、不修复平台、不自行选定污染物或病毒。
