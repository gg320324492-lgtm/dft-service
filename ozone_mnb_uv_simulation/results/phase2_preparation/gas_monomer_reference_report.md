# JOB-2026-0906-024：NH3/O3 同方法气相单体参考结构批 · 汇总报告

## 本批在做什么 / 已完成什么 / 还需要什么

- **在做什么**：为后续 NH₃+O₃ 加合物研究建立同一项目方法（wb97xd+项目−D2 全导数/def2-TZVP/L8/grid_response/SCF 1e-12,1e-9，气相）下的单体参考结构与电子能。**两个单体限定**：不启动加合物、产物通道、TS、IRC、高层级计算，不算 Hessian/频率。
- **已完成**：计算前补正（任务书 v1.1、电子态表述、S13 Table 1 视觉核对、"(1)"降级为疑似重复）；计算前无 SCF 检查（全通过）；两单体受控优化 + 独立端点复核 + RKS 稳定性分析；**两单体均达到驻点候选判据（max|g|≤1e-5）**。
- **还需要**：两单体的频率验收（下批）；加合物建模（前须视觉核对 S13 结构图的坐标数据）；水体类型/pH/波长（不阻塞本批）。

---

## 1. 计算前检查（无 SCF，`run_artifacts/02_nh3o3_reference/precheck_noSCF.json`）

| 检查 | 结果 |
|---|---|
| D2 参数覆盖 N/O/H | ✓（C₆: H 0.14/N 1.23/O 0.70；R₀: H 1.30/N 1.55/O 1.52） |
| D2 能量—梯度一致性（解析 vs FD，无 SCF） | ✓（最大误差 6.6×10⁻¹⁵ Eh/Bohr） |
| 原子顺序/电荷/自旋/单位往返 | ✓（NH₃: N,H,H,H；O₃: O,O,O；charge 0/spin 0；坐标与输入差 <1e-10 Å） |
| NH₃ 非平面初猜 | ✓（C₃v 锥形，r=1.012 Å、∠HNH=106.7°，标准几何值来源） |
| O₃ 起点可追溯 | ✓（`models/00_baseline/o3/optimized_gas.xyz`，历史 wb97X-D 时代基线几何，r=1.2379 Å、∠118.13°，历史来源标签保留） |

## 2. 执行历程与预算审计（重要偏差如实登记）

| 运行 | 求值 | 说明 |
|---|---|---|
| v1（NH₃ 优化） | 15 | **我方驱动缺陷**：预求值补丁把梯度绑定在初始几何的 Gradients 对象上（GradScanner 经 `__dict__` 拷贝后未更新 mol）→ 梯度冻结（恒 4.357×10⁻²）→ pyberny 信任半径崩溃。15 次求值浪费 | **【025 根因表述更正】早期"pyberny 信任半径崩溃是根因"的判断已被取代：信任半径崩溃是**驱动缺陷（梯度冻结）的下游结果**；与优化器自身收敛阈值问题（v4：内坐标判据 4.5e-4 已满足而笛卡尔 1e-5 未达——阈值差异，非缺陷）相区分。**
| v1 端点复核 | 1 | 新对象在同一几何给出 4.634×10⁻²（与优化器 4.357×10⁻² 不同——提示异常） |
| O₃ 优化 + 复核 | 2 | 从历史基线几何起步，**一步即达 3.514×10⁻⁶**（新方法下该几何已收敛）；复核逐位一致 |
| 诊断（一致性检验） | 3 | 确定性 ✓（重复差 1.7×10⁻¹² Eh）；**能量—梯度一致性 ✓**（沿 −g 位移 0.05 Bohr → ΔE=−0.00330，预测 −0.00306）→ 排除路径不一致，定位为优化器过早停止 |
| v2 续跑 | 7 | 补丁缺陷未除，梯度仍冻结 |
| v3 续跑（**移除补丁**） | 5 | 梯度正常变化：4.634e-2→1.855e-2→1.624e-3→**6.484e-5**（pyberny 自身判据满足） |
| v4 续跑 | 2 | pyberny 即判收敛；复核仍 6.484e-5 > 1e-5 |
| v5 续跑（**收紧收敛参数** gradientmax=3e-5 等） | 4 | 6.484e-5→3.236e-5→**7.661×10⁻⁷**；独立复核 ✓ |
| **合计** | **39 / 42** | 剩余 3 |

**偏差登记**：①NH₃ 优化求值超出"每单体 ≤20"分配（实际 ~29，其中 15 次浪费于上述驱动缺陷，其余用于收敛收紧）；②NH₃ 端点复核执行了 3 次（v1/v4/v5——前两次分别因优化器崩溃与判据未达而失效，v5 为有效交付）；③账本粒度缺陷（019 机制的 attempt 挂在 scanner 创建级而非逐求值）已发现并按逐求值修正。**总上限 42 未突破（39/42）——但总上限未突破不使分项超限合规**：NH₃ 优化超额（>20）、3 次端点复核（原令 ≤1）、3 次诊断求值与 4 次续跑（均未获原批令预先安排）已**分别登记**为未获批/超分配项，历史证据全部保留。**【025 补正】删除"超额被总预算吸收"类表述**——总预算余量不构成分项超限的合规依据。

**根因记录（供后续驱动沿用）**：pyberny/GradScanner 路径下**禁止**对 `nuc_grad_method` 返回对象做 kernel 补丁（`__dict__` 拷贝导致梯度冻结于初始几何）；优化循环内的预算记账应使用回调后置记录 + maxsteps 硬上限（本批 v3–v5 即此模式）。

## 3. 单体参考结果（本项目方法，气相）

| 单体 | 电子能 E（Eh，全精度） | max\|g\|（未投影） | 判定 | RKS 稳定性 |
|---|---|---|---|---|
| NH₃ | **−56.564219136** | **7.661×10⁻⁷** | **驻点候选 ✓**（≤1e-5） | **【025 补正】保存的返回项不足以判断内部稳定性（见下）** |
| O₃ | **−225.433902700** | **3.514×10⁻⁶** | **驻点候选 ✓** | **【025 补正】保存的返回项不足以判断内部稳定性（见下）** |
| **同方法分离单体参考能之和** | **−281.998121836** | —— | 供后续加合物计算参考 | —— |

- D2 贡献：NH₃ 与 O₃ 的 E(−D2) 有符号值已逐步保存（`nh3_v5_steps.json`、O₃ 步记录）。
- **判定标签**："驻点候选"（未经投影完整梯度 ≤1e-5 + 优化器自身判据 + SCF 收敛 + 独立新对象复核一致）——**未算 Hessian/频率，不是已验收极小值**；**无 ZPE/焓/自由能**。
- **RKS 限制登记（025 补正）**：经源码核实（pyscf/scf/stability.py），`mf.stability()` 默认 `return_status=False`，返回 `(mo_i, mo_e)`——**稳定性布尔值（stable_i/stable_e）仅在 return_status=True 时返回**；我方调用未带该参数，保存的 st[1] 为 mo_e=None（未请求外部检查的产物）。**因此"None"不构成"未发现内不稳定"的证据，应读作"此次保存的返回项不足以判断内部稳定性"**；后续如需判定，须以 `mf.stability(return_status=True)` 保存 stable_i/stable_e。内部稳定性 ≠ 多参考排除；<S²>=0 同理。（O₃ 已知多参考，S13 R 行 T1=0.0346 为组合反应物值、不归为孤立 O₃）。

## 4. 补正记录汇总（计算前完成）

`notes/job024_premcomputation_corrections_2026-09-07.md`：任务书 v1.1（C/D 移出、13–23 粗估撤回）；电子态表述 4 条收紧；S13 方法段与 Table 1 表头/脚注/R 行视觉核对（R 行 T1=0.0346 属组合反应物、T1 在 CCSD 级、Table 1 与 Table 3 的 ZPE 口径可能不同——均按此引用）；"(1)"版本对降级为"疑似同内容重复"。

## 5. 未完成项

1. NH₃/O₃ 的**频率验收**（虚频=0 判据）——下批；
2. **加合物研究**——前须视觉核对 S13 Figure 1 的结构坐标（本批未做）；
3. RKS 充分性的进一步确认（多参考诊断 T1 需 CCSD 级计算——不在本批）。

## 6. 结论边界

两单体结果为**本项目方法、气相**的驻点候选参考；不用于任何养殖场景结论；不与 S13 数值混比（方法不同）；水相扩展与病毒/界面方向均不在本批。
