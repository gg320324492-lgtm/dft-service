# JOB-040 审计、补正与执行控制修复报告（2026-09-08）

## 一、JOB-039 逐 attempt 审计（`attempt_audit.json`；039 文件哈希快照已存）

| attempt | 触发来源 | SCF 启动 | SCF 完成 | 梯度完成 | 当时授权符合性 |
|---|---|---|---|---|---|
| [0]（E=−282.00171974160537，与 038 末接受点参考值精确一致） | **测试**（T2 初版 mock 目标错误：patch `so.minimize` 而非模块内 `W.minimize` → 真实后端对正式路径执行） | 是 | 是 | 是（完整 7×3 已存） | **违规**（测试代码读写正式路径） |
| [1]（pre_checked） | 测试（同一次运行；线搜索试探） | **无法确定**（120 s 工具超时 SIGTERM 杀进程；无记录写入） | 无法确定 | 否 | **违规**（同一次运行） |
| [2]（门失败试探点，距起点 2.1×10⁻⁴ Å） | **正式入口**（039 续优化运行） | 是 | 是 | 是 | **流程缺陷下的未授权求值**：起点门未闭合（起点记录复用绕过门），门在第一个新点触发；有剩余预算不构成授权 |

## 二、计数补正（撤回口径）

- **撤回** JOB-039"本批新增真实求值仅 1 次"——不完整口径；
- 实际成本汇总：**预算 attempt 3；确认启动 SCF 2 + 无法确定 1；完成 SCF＋梯度 2；不确定 1**；
- **误触发计算（测试引发）全部计入实际成本**（attempt 0 完整 + attempt 1 不确定）；
- 状态名称不证明计算未发生：attempt 1 按证据标"**无法确定**"，不重算补证。

## 三、科学结论限定

1. 039 **未完成预定续优化对照**；
2. **起点门被误用于其他几何**（线搜索试探点不适用起点能量/梯度一致性要求）；
3. **不据此否定 038 端点的可复现性**（037 已独立验证）；
4. 隔离违规后的修复与继续执行经过已如实登记（本轮审计与修复）；**后续一旦发现越界真实调用，必须停止整个正式执行流程**。

## 四、执行控制修复（新入口 `protocols/c1_cart_exec_v3.py`；039 脚本留档未改）

1. **显式依赖注入**：测试只能取得模拟后端，无自动回退；正式执行需显式 `--execute --backend real --init-new-ledger`；
2. **导入零副作用**：模块导入不建目录、不开账本、不启动计算（BATCH_DIR 仅常量，目录创建移入执行函数）；
3. **路径守卫**：解析后路径位于正式目录时**写入前拒绝**（PathGuardError；测试误传正式路径被拒）；
4. **移除隐式正式默认值**：records/results 路径为必传参数；
5. **"禁止真实调用"哨兵**：未 mock 优化器时仍只调用模拟后端（T7：哨兵工厂计数验证，真实后端不可达）；
6. **缺失账本拒绝静默新建**；正式执行不自动重启。

## 五、起点门移到优化器启动之前

执行顺序（已实现）：**来源与单位核验 → 起点完整记录验收（1 次独立求值，全记录落盘）→ 门通过 → 启动优化器 → 普通试探点求值（仅一般检查）**。

- 门函数显式接收指定起点与参考记录（不依赖缓存命中顺序）；
- 门未通过 → StartGateError，优化器不启动（T2 验证：错误来源/单位/配置下优化器调用 0）；
- 起点验收完成并安全落盘后，优化器首次请求同一坐标**复用已验收记录**（不重复消耗 SCF，T1 验证：0 次新 SCF）；
- 合格起点记录复用后，第二个不同几何**不重触发起点门**（T4 验证：仅 1 次新 SCF 的一般检查）；
- 恢复：仅当完整结果/来源/单位/配置/哈希/验收凭证均兼容（T5a/T5b 验证：篡改哈希拒收；未完成 attempt 不猜测）；
- 下一批正式计算仍须按批令执行一次独立起点复现，**不用 039 违规遗留缓存代替**。

## 六、无 SCF 测试（`test_c1_cart_exec_v3.py`，逐项断言全部通过）

| 项 | 断言与触发方式 | 结果 |
|---|---|---|
| T1 | 正确起点验收（模拟后端 dE=1.5×10⁻¹²）→ 优化器首调复用记录（0 新 SCF） | ✓ |
| T2 | 错误来源（033 末步）SourceError；错误单位（物理几何 5.95 Å ≠ 3.1493）；配置不符 → 优化器启动 0 次 | ✓ |
| T3 | 起点缓存存在但无验收凭证 → run_optimizer 拒绝启动，SCF 调用 0 | ✓ |
| T4 | 合格起点复用后第二几何 → 不重触发门，仅 1 次新 SCF 一般检查 | ✓ |
| T5a | 篡改记录哈希（coords_bohr_sha='deadbeef'）→ StartGateError 拒绝复用 | ✓ |
| T5b | pre_checked attempt → 不猜测完成，新 SCF 执行 | ✓ |
| T6 | 测试写正式路径 → PathGuardError，文件未创建 | ✓ |
| T7 | 优化器未 mock（真 scipy.minimize）→ 哨兵工厂仅模拟调用（3 次），真实后端不可达 | ✓ |
| T8/T8b/T8c | 预算耗尽（total=2 第 3 次 SCF 前拒绝）；后处理异常（21 分量梯度可恢复）；持久化异常（/dev/null 路径，无部分写入） | ✓ |
| T9 | 条件复核路径使用完整保存机制（验收记录含 21 分量梯度，结构性验证） | ✓ |
| T10 | 测试前后正式目录清单与哈希不变（files=0，目录未创建） | ✓ |

**本批新增真实量化计算：0。**

## 七、下一批执行流程草案（待批）

```
# 正式 ftol=1e-13 续优化（新批授权后）：
python protocols/c1_cart_exec_v3.py --execute --backend real --init-new-ledger
```
流程：来源核验（034 末步）→ 单位链 → **起点验收 1 次求值**（门限 |ΔE|≤1e-8、梯度标量 ≤1e-7）→ 门过 → L-BFGS-B（gtol=1e-6/ftol=1e-13/maxcor=10）≤20 优化 attempt → 任一点 max|g|≤1e-5 受控停止 → 独立复核 1 次（同完整保存机制）→ 通过则登记"驻点候选，稳定性与频率待查"。
**预算**：总 attempt ≤21（起点验收 1 + 优化 20）；复核 1 另计但受总上限约束；失败计入。

**本批停止。** 量化操作 0；等待下一批授权。
