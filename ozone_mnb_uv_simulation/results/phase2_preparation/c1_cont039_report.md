# 收紧能量停止条件的有限笛卡尔续优化批 · 汇总报告（JOB-2026-0906-039）

## 本批在做什么 / 已完成什么 / 还需要什么

- **在做什么**：从 JOB-038 最后接受点（a960f4a403fbd3ec）继续全自由度笛卡尔优化，检验收紧能量停止条件（ftol=1e-13）后能否推进梯度收敛。
- **已完成**：JOB-038 补正（4 项，含停止判据核算与梯度冻结归属更正）；续算起点核验（a960f4a403fbd3ec 属接受点清单、E/gmax 与总指挥数字精确匹配）；零求值检查 5 项全过；**正式运行在首点门处硬停——门未通过，按批令立即停止**。
- **还需要**：总指挥决定续优化门逻辑修正后是否重新授权（§5）。

---

## 1. JOB-038 补正（详见 `notes/job039_job038_corrections_2026-09-08.md`）

1. "7/20 次真实求值" → "**7 次预算 attempt（6 次完成 SCF＋梯度、1 次 SCF 前失败）**"（缺失导入异常，SCF 未启动）；
2. **撤回"达到机器精度"与"平坦势盆"**：按实际接受点能量核算停止判据——L-BFGS-B 相对能量阈值 = ftol×|E| = 2.22×10⁻⁹ × 282.0017 ≈ **6.26×10⁻⁷ Eh**；最后一次接受步（attempt 5→6）能量下降 = **4.29×10⁻⁸ Eh** < 6.26×10⁻⁷ → **按设定阈值停止**（非机器精度、非势盆证据）；
3. **删除"pyberny 约 3×10⁻⁴ 地板"**——不做跨起点/预算/停止条件的优化器优劣宣布（保留本批起点 7.6×10⁻⁴ → 最小梯度点 1.71×10⁻⁴ 的数值变化）；
4. 038 首次运行缺失导入崩溃后的续跑**未经总指挥批准**——与原批令硬停边界对照登记（失败 attempt 已计入预算）。

## 2. 续算起点核验

- 起点 = 038 最后接受点 **a960f4a403fbd3ec**：E=−282.0017197416054、max|g|=1.935298044405478×10⁻⁴（与总指挥数字精确匹配）；确认属于 038 接受点清单；来源哈希 53e0748b14b814a3（按授权文件重算）；
- 续算输入：`inputs/nh3o3_phase2/c1_cont039/cont039_start.xyz`（含完整梯度参考）；
- 明确登记：**几何续算、优化器状态重置**（不称恢复 Hessian 历史）；
- 设置（运行前记录）：L-BFGS-B、**ftol=1e-13（收紧）**、gtol=1e-6、maxcor=10、maxiter=15000；不平移能量零点、不缩放目标函数或梯度；全部 Cartesian 变量、无约束、不投影、不逐步配准；方法/单位链/网格/SCF 与 038 一致。**这是收紧软件停止条件，不是放宽驻点验收标准，也不保证收敛。**

## 3. 零求值检查（5 项全过，`test_c1_cart_cont039.py`）

T1 正确起点与单位链（dev=4.6×10⁻¹³ Å）✓；T2 实际 ftol=1e-13/gtol=1e-6 到达优化器 ✓；T3 测试输出仅写 TMP ✓；T4 错误起点（033 末步）被哈希检查拒收 ✓；T5 预算耗尽在 SCF 前拒绝 ✓。
**测试自身暴露并登记的隔离违规**：T2 初版 mock 目标错误（`so.minimize` 而非模块内 `W.minimize`），导致该测试版本以真实后端对正式路径执行了真实求值——**隔离违规已登记**（见 §4）；修正后 T2 通过且零求值。

## 4. 正式运行：首点门未通过 → 硬停（照实登记）

**账本状态（3 attempts）**：

| attempt | 类别 | 状态 | 说明 |
|---|---|---|---|
| 0 | opt | done | E=−282.00171974160537——**数值上精确等于 038 最后接受点参考值**（来自 T2 初版隔离违规运行的真实求值，已登记） |
| 1 | opt | pre_checked | 隔离违规运行中被 SIGTERM 中断，SCF 未完成 |
| 2 | opt | error | 本批正式运行的首个**新**求值（线搜索试探点）：**首点门未通过** |

**首点门失败详情**（attempt 2，新求值点距起点 2.1×10⁻⁴ Å 的线搜索试探点）：
- dE = 6.27×10⁻⁸ Eh（门限 ≤1×10⁻⁸）— 未过；
- dcoords = 2.10×10⁻⁴ Å（门限 ≤1×10⁻⁹）— 未过；
- dgrad_max = 3.09×10⁻⁴ Eh/Bohr（门限 ≤1×10⁻⁷）— 未过。

**按批令：首点未通过，立即停止**——未进入优化；**复核额度未使用**；不自动修复后续算。

### 门失败的根因分析（供下一批修复，本批不自行修复）

1. **门逻辑对续算场景的设计缺陷**：起点坐标的完整记录已存在（隔离违规运行所留），续算首调**复用**该记录并绕过门检查；门随后在第一个**新**求值点（线搜索试探点，距起点 2.1×10⁻⁴ Å）触发——试探点本就不是起点坐标，**按门定义必然失败**。
2. 修正方向（下一批）：门应仅在**起点坐标本身**的求值上执行（新鲜求值或核验通过的复用记录视为门通过），线搜索试探点不适用起点门。
3. **隔离违规登记**：T2 测试初版 mock 目标错误导致 2 次真实求值进入正式账本/记录（attempt 0 done 与 attempt 1 pre_checked）——**违反"测试不得读写正式账本"**；原始记录保留，违规求值不计入本批授权的对照证据。

### 本批求值计数（分别统计）

- 预算 attempt：**3**（上限 21 内）；
- 实际启动 SCF：**2**（attempt 0 与 attempt 2）；
- 完成 SCF＋梯度：**2**（attempt 0——隔离违规运行所留但数据合法；attempt 2——本批授权的门检查求值，门未通过）；
- 复核：**0**；稳定性/Hessian/频率：**0**。

## 5. 下一步（待总指挥批准，本批不执行）

修正续优化门逻辑（起点门仅在起点坐标求值上执行；核验通过的复用记录视为门通过；线搜索试探点不适用起点门）并修复测试隔离（mock 目标 W.minimize），然后重新授权本续优化。门逻辑修正后，预期首调（复用或新鲜）通过门，优化器正常继续。

## 6. 边界

本批新增真实求值 **1**（门检查，未通过门）；预算 attempt 3/21；不计算 Hessian、频率、CP、TS、IRC、高层级或水相结果；历史记录与错误证据保留；未修改 DFT 平台、依赖或 PySCF 安装。

**本批停止。**
