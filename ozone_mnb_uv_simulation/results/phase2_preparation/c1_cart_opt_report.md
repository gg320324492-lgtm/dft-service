# 完整求值持久化修复与有限笛卡尔优化对照批 · 汇总报告（JOB-2026-0906-036）

## 本批在做什么 / 已完成什么 / 还需要什么

- **在做什么**：修复原始坐标、能量和完整梯度的保存机制，然后从 JOB-034 末步几何尝试一次笛卡尔优化，检查另一种优化策略是否能推进收敛。
- **已完成**：JOB-035 补正（5 项）；完整持久化机制修复 + 回归测试 5/5；**笛卡尔 L-BFGS-B 对照 20/20 求值用尽（预算硬停）——未改善**；无点达复核阈值 → 复核 0。
- **还需要**：总指挥依据两套策略的完整证据决定后续路线（§5）。

---

## 1. JOB-035 补正（详见 `notes/job036_job035_corrections_2026-09-08.md`）

1. "完整梯度复现通过" → "**能量和已保存的梯度标量复现；历史完整梯度缺失，完整向量复现无法核验**"；
2. 删除"逐位复现"及排除全部调用路径/SCF 分支差异的过强表述；
3. 删除"强非谐性"判断——**数值核算**：前向斜率 ≈ 中心导数 + 曲率×h/2 = −1.1390×10⁻³ + 0.5075×0.001 = −6.31×10⁻⁴，与 h=0.002 观测 −6.3148×10⁻⁴ **相符**（局部二次近似一致，不能判断强非谐）；
4. 删除"瓶颈已确定为 pyberny 内坐标判据"——下一步是**策略对照**，不是已确诊故障后的修复；
5. 如实登记：**重复中心计算 1 次 + 缺失位移点（minus_h0.002）**——总次数未超 5 ≠ 完成原定计算组成；**缺失的梯度向量不能从 grad_max/grad_rms 恢复**。

## 2. 完整持久化修复（回归测试 5/5 通过）

每次真实求值后**立即**原子保存完整记录：实际计算坐标（Bohr 变量 + Å 存储）、元素序与单位、完整能量与 D2 项（e_total/e_d2_analytic/e_dft_part）、完整 N×3 梯度（Eh/Bohr）、实际配置、SCF 状态、attempt 号；**安全写盘后才标记 done** 再进行分解/方向构建/报告格式化；后处理失败时完整记录仍可读（T-A1）；done 标记仅在安全落盘后（T-A2）；重启仅复用坐标/配置/记录哈希均核验一致的结果（T-A3）；配置变化不复用（T-B）；总上限求值前拒绝（T-C）。测试注入点：求值成功后人为触发后处理异常。

## 3. 笛卡尔策略对照（20/20 求值，预算硬停）

- **起点**：JOB-034（c1_full_opt_cont）step 20 最后已完成求值实际几何（文件身份与哈希核验：650e5246519bcb69；**不使用诊断位移点**）；
- **设置（运行前记录）**：SciPy L-BFGS-B，全部 21 个 Cartesian 坐标（Bohr）为变量，无约束、不投影梯度、不旋转配准；gtol=1e-6、ftol=2.22e-9、maxcor=10、maxiter=15000（**maxiter 不是求值上限——硬限制来自求值前持久化账本**）；方法/基组/网格/SCF/D2 与 JOB-026/031/033/034 完全一致；fun 单次调用同时返回 Eh 能量与 Eh/Bohr 完整梯度（同一几何）；
- **结果**：20 次求值后账本硬停（scipy 未返回）；**未改善**：

| 量 | JOB-034 起点 | 笛卡尔 20 次后最佳 |
|---|---|---|
| max\|g\| (Eh/Bohr) | **3.02×10⁻⁴** | 2.21×10⁻³（全部保存点中最佳） |
| E (Eh) | −282.001717 | −282.001257（全部保存点中最低） |

- 轨迹特征：L-BFGS-B 单位初始 Hessian 在近收敛几何上产生大步（eval 2–9 能量升至 −281.98），随后游走，未回到起点的梯度水平；
- **无保存点达到 max|g|≤1e-5** → 独立复核 **0 次**（触发条件未满足）；
- 能量小变、优化器 success、线搜索停止均未替代完整梯度验收。

## 4. 判定（按批令）

- **登记未收敛 + 有限对照未完成**（达预算上限，不外推还需几步）；
- 若改善只说明该起点和设置下策略有效——**本批未改善**，同样**不自动宣布数值地板或放宽标准**；不证明 pyberny 有根源性错误（未改善仅说明该起点/该设置下此策略无效）；
- 证据现状（供路线决定）：pyberny 内坐标 50 次求值 → max|g| ~3×10⁻⁴（两次独立初始化一致）；L-BFGS-B 笛卡尔 20 次求值 → 2.2×10⁻³（未回到起点水平）；残余梯度来源仍未定位。

## 5. 求值计数

- 笛卡尔优化实际求值 **20/20**（含初始点与全部线搜索点；预算硬停）；
- 独立端点复核 **0**（无点达 max|g|≤1e-5，触发未满足）；
- 模拟测试求值 0（持久化回归为无 SCF 测试）。

## 6. 边界

本批不运行稳定性分析、Hessian 或频率；不启动其他构型、反应通道、水相或高层级计算；原始证据保留（033/034/035 全部轨迹与账本）；未修改 DFT 平台、PySCF 安装或桌面文献原件。

**本批停止。**
