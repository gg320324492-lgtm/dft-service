# 笛卡尔优化起点来源、单位与记录隔离修复核验批 · 汇总报告（JOB-2026-0906-037）

## 本批在做什么 / 已完成什么 / 还需要什么

- **在做什么**：修复 036 的起点来源、单位传递和测试记录混入问题，独立复现 034 真正末步。**真实 SCF+梯度上限 1 次；其他量化操作 0 次。**
- **已完成**：零求值溯源（四项发现全部确认并量化）；036 结论撤回与记录审计；输入链修复 + 5 项无 SCF 回归全过；**1 次复现求值完成——034 真实末步复现成功（全部门限通过）**。
- **还需要**：总指挥授权后另行决定是否重新进行笛卡尔优化对照（本批不自动续算）。

---

## 1. 零求值溯源（详见 `notes/job037_traceback_and_audit.md`）

| 总指挥发现 | 核实结果 |
|---|---|
| 036 读错起点 | **确认**：cont_start.xyz 与 **033 step 30** 一致（偏差 4.9×10⁻¹³ Å）；与 034 step 20 差 0.106 Å。根因：034 批提取脚本在 cont 结果文件尚不存在时运行，`n_opt_steps==30` 断言只能被 033 文件满足——来源核验形同虚设。**撤回"034 末步来源核验通过"** |
| 坐标单位错用 | **确认**（以实际分子对象量化）：Å 数值直传 `unit='Bohr'` 后 N—中心氧距离 = **1.6770 Å**（预期 3.1691），比值 **0.529177**；最大原子间距比值同——整体缩小 0.529 倍 |
| 首个真实求值 | cart_run.log：`eval 1 E=−275.008373537 max|g|=8.375e+00`（缩放结构的爆炸梯度）。**撤回**"正确起点下优化策略无效"与"初始 Hessian 大步导致失败"归因——20 次求值全部在缩放错误结构上 |
| 034 末步参考值 | E = **−282.00171709790953**；max|g| = **7.59518455×10⁻⁴**；grad_rms = **2.48550242×10⁻⁴**（原始记录精确值，来源已登记） |

## 2. eval_records.json 审计（原文件保留；分类索引 `eval_records_classification.json`）

- **22 条 = 20 真实 + 2 测试**：
  - 20 条真实（attempt 0–19，E ~ −282 尺度，对应正式账本 20 次 done）——计入历史成本，**不作为预定起点的优化器对照证据**（对象为缩放错误结构）；
  - 2 条测试混入：attempt=0（E=−0.99375，036 首轮测试 pre-records_path 写入）、attempt=1（E=+0.40625，**T-C 测试漏传 records_path** 写入）——**不计作真实 SCF**；
  - 分类规则非能量筛选：E 标度 + e_d2 有限性 + attempt 关联；
  - 正式账本 budget_cart.json 干净（20 attempts 全部对应真实记录；测试记录关联测试账本）。

## 3. 036 结论撤回清单

| 036 表述 | 处置 |
|---|---|
| "起点核验通过（JOB-034 step 20）" | **撤回**（实为 033 step 30） |
| "笛卡尔策略在该起点未改善" | **撤回**（对象为缩放错误结构） |
| "L-BFGS-B 单位初始 Hessian 大步导致先升后游走" | **撤回**（同上） |
| "不证明 pyberny 根源错误、不宣布数值地板" | 保留（本就不成立） |
| "证据现状：L-BFGS-B 20 次→2.2e-3" | **修订**：不适用于预定起点对照，仅计历史成本 |

**保留但不适用于预定对照**：036 的 20 次真实求值原始记录（缩放结构上的合法 SCF 数据）；测试记录（程序测试数据）。

## 4. 输入链修复（新入口 `protocols/c1_repro_034.py`；旧执行版本 `c1_cart_opt.py` 留档）

- 直接读取 034 结果**最后一次已完成求值**坐标（不经旧 cont_start.xyz）；来源清单含批次/步号/坐标单位/元素顺序/源文件哈希/原始参考标量（精确值）；
- **来源检查**：033 末步哈希 → 明确拒绝；未注册哈希 → 拒绝；034 末步哈希 + 能量/梯度标量精确匹配 → 通过；
- **显式 Å→Bohr 转换**（采用 **PySCF 自身 Bohr 常数** 0.52917721092——CODATA 值在第 9 位的差曾使正确路径的读回偏差达 1.9×10⁻⁹ Å 而触发 1e-9 门限）；实际对象读回 Å 与源比较 dev=2.6×10⁻¹¹ ≤1×10⁻⁹；
- 能量与梯度来自同一实际几何；完整 N×3 梯度及单位保存；
- 缓存身份含单位/实际坐标/元素顺序/完整配置/**后端身份**；结果路径参数化（修复硬编码）；正式/测试目录完全隔离；
- 账本缺失拒绝静默新建；显式 init；失败计入。

## 5. 无 SCF 回归（5 项全过，`test_c1_repro_034.py`）

| 测试 | 结果 |
|---|---|
| T1 误用 033 末步 → 来源检查拒绝 | ✓ |
| T2 漏做/重复做 Å→Bohr 转换 → 实际几何检查拒绝（dev：正确 2.6×10⁻¹¹、漏转换 3.67 Å、双转换 10.61 Å） | ✓ |
| T3 真正 034 末步经完整输入链物理几何保持（N—中心氧 3.1493 Å 一致至 1e-9） | ✓ |
| T4 模拟测试仅写 TMP、正式目录前后哈希不变 | ✓ |
| T5 持久化失败/重启/缓存不兼容/预算耗尽均正确停止 | ✓ |

修复过程中由测试暴露并当场修复的入口缺陷：结果路径硬编码（2 处）、后端身份不可序列化、mf 未定义分支、缺失 load_json。

## 6. 一次起点复现（授权 1 次，已执行）

**034 真实末步几何，同方法同配置（wb97xd+项目−D2 附着/def2-TZVP/L8/grid_response/SCF 1e-12,1e-9/气相/RKS），独立新对象 1 次 SCF+完整梯度：**

| 量 | 034 原始记录 | 本次独立复现 | 差 | 门限 | 判定 |
|---|---|---|---|---|---|
| E (Eh) | −282.00171709790953 | −282.001717098 | **5.1×10⁻¹³** | ≤1×10⁻⁸ | ✓ |
| max\|g\| (Eh/Bohr) | 7.59518455×10⁻⁴ | 7.595185×10⁻⁴ | **8.6×10⁻¹²** | ≤1×10⁻⁷ | ✓ |
| grad_rms (Eh/Bohr) | 2.48550242×10⁻⁴ | 2.485502×10⁻⁴ | **2.6×10⁻¹²** | ≤1×10⁻⁷ | ✓ |

- SCF 收敛、全部有限、实际坐标与来源一致（≤1×10⁻⁹ Å）；
- **完整 7×3 梯度向量已作为新记录保存**（`repro_records.json`）——**不称"完整向量复现"或"逐位复现"**（历史完整向量缺失，仅标量可比）；
- 验收：**034 真实末步已复现**（门限全过）；
- 通过即停止——本次额度未用于线搜索、位移、优化或额外复核。

## 7. 边界

**本批真实 SCF+梯度 1/1**；其他量化操作 0；036 原始文件与错误证据保留未覆盖；不修改 DFT 平台源码、平台配置、依赖或 PySCF 安装（已有证据指向项目驱动，非平台问题）。

**本批停止。** 下一批需总指挥授权：是否以 034 真实末步（现已有核验起点与完整梯度新记录）重新进行笛卡尔优化对照，或转向其他路线。
