> **[JOB-047 补正]** 本报告第 3/4 节部分表述已修正："24.9% 解释占比"改为> 绝对量级比（不称消除差额）；删除"剩余 1.04×10⁻⁴"的错误减法，保留两个
> 带符号差额（k_H−k_on=−1.3886×10⁻⁴、k_H−k_off=−1.7345×10⁻⁴）；D2 一致性
> 结论限定当前几何/方向/步长；RSH 仅列待定假说；原始未对称矩阵缺失不作
> 固定方向二次型差异的独立解释；−72.186 cm⁻¹ 继续标为保存矩阵预测值。
> 详见 `notes/job047_046_correction.md`。

# JOB-2026-0906-046 报告：C1 负模方向网格响应贡献核查

**日期**：2026-09-09 · **预算**：SCF 2/2、完整梯度 4/4（True/False × 2 几何）、纯 D2 解析梯度 2/2（单列，无 SCF）——**全部完成、0 失败**；优化/稳定性/电子 Hessian/D2 Hessian/频率重算均为 0。
**前置**：q 哈希三方核对一致（`59599f2a2c0be260`）；几何直接取 045 实际计算坐标（坐标往返 ≤4.3e-11 Bohr、最小原子间距 1.911 Bohr）；模拟后端测试 11/11 全过（开关到达梯度对象/无重复 SCF/真实后端屏蔽/正式目录哨兵不变）。

## 1. 复现门（True 梯度 vs 045 同点）——两几何全 PASS

| 点 | dC (Å) | \|ΔE\| (Eh) | 梯度逐分量最大差 (Eh/Bohr) | 判定 |
|---|---|---|---|---|
| +0.01 | 0.0 | 2.84×10⁻¹³ | 1.34×10⁻¹² | ✓ |
| −0.01 | 0.0 | 5.68×10⁻¹³ | 2.51×10⁻¹² | ✓ |

梯度对象读回：`Gradients_D2` 类、grid_response=True/False 显式生效、D2 两条路径均只附加一次；SCF 配置与 045 相同（wb97xd+D2、L8、1e-12/1e-9、def2-TZVP、气相）；chkfile 已存（p01/m01，178 KB）。

## 2. 源码只读核查（PySCF 2.14.0）

- **梯度侧**：`grad/rks.py:745` `grid_response = getattr(__config__, ..., False)` **默认 False**；开启时两条路径生效——`get_veff` 用 `get_vxc_full_response`（行 50–56）、`extra_force` 加入 `exc1_grid` 网格响应项（行 772–780）。
- **Hessian 侧**：`hessian/rks.py` 中 grid_response 字样仅出现在 **NLC 数值二阶导**相关代码（行 461 注释、471/482 调用、573–624 辅助函数）；**实测 `libxc.is_nlc('wb97xd') = False`** → 这些路径对 wb97xd 不触发。**普通 XC 解析 Hessian 不含网格权重/格点位置随核坐标的响应项**（"冻结网格"意义的 XC 曲率）。
- 不能仅凭属性名判断导数项构成——以上均以源码行号+调用关系为证；未修改任何安装源码。

## 3. 曲率比较（h = 0.01 Bohr，单位 Eh/Bohr²）

| 量 | 值 |
|---|---|
| k_on（总，resp=True） | **−6.9723×10⁻⁵**（复现 045 k_g(0.01) 至 **5.9×10⁻¹³**） |
| k_off（总，resp=False） | **−3.5133×10⁻⁵** |
| **Δk_grid = k_on − k_off** | **−3.4590×10⁻⁵** |
| k_d2 解析曲率（一致性检查） | +3.49600×10⁻⁶（vs 043 D2 FD Hessian 参考 +3.49605×10⁻⁶，差 4.6×10⁻¹¹ ✓） |
| k_on_dft / k_off_dft（扣 D2） | −7.3219×10⁻⁵ / −3.8629×10⁻⁵（Δk_grid_dft = −3.4590×10⁻⁵，与总口径逐位一致） |
| **k_H − k_on（原差额）** | **−1.3886×10⁻⁴** |
| **k_H − k_off（关响应差额）** | **−1.7345×10⁻⁴** |
| 解释占比 Δk_grid/(k_H−k_on) | **24.9%** |

**沿 q 等效频率**（μ_eff = 1.0577 amu）：k_off → **−29.87 cm⁻¹**；k_on → −42.07 cm⁻¹；k_H（043 矩阵）→ −72.19 cm⁻¹。

## 4. 判读

1. **网格响应本身量级显著**：Δk_grid = −3.459×10⁻⁵，占开响应曲率量级的 **49.6%**——在该软模方向上，网格响应把有限差分曲率**加大约一倍**（更负）。这个效应真实存在且此前从未被单独计量。
2. **但本批不支持"网格响应差异解释主要差额"**：k_off（−3.51×10⁻⁵）比 k_on **更远离** k_H（−2.09×10⁻⁴）；Δk_grid 只解释原差额的 **24.9%**，剩余 **~1.04×10⁻⁴（75.1%）未解释**。按任务书判读分支，登记"**差额仍明显**"并停止——不扩大网格、不追加步长、不重算 Hessian。
3. **D2 侧嫌疑被本批正面排除**：解析 d2_grad 沿 q 的曲率与 043 D2 FD Hessian 参考一致至 4.6×10⁻¹¹——**矛盾全部位于 DFT 部分**（k_H_dft = −2.1208×10⁻⁴ vs k_off_dft = −3.8629×10⁻⁵，~5.5 倍）。
4. **登记待定假说**（不结论）：H3：ωB97xd 为 range-separated hybrid（已实测 is_rsh=True），PySCF 解析 Hessian 的 RSH 二阶导实现对该体系软模方向可能存在方法学局限；H2（沿承 044/045）：原始未对称化矩阵缺失，逐元素审查能力受限。关闭梯度响应的有限差分**不自动等同于**完整解析 Hessian，两者的差额不能直接归因于单一缺失项。
5. **管线一致性副产品**：k_on 与 045 k_g(0.01) 重合至 5.9×10⁻¹³——独立 SCF 重跑下梯度逐分量差仅 ~10⁻¹² 级，045 求值记录与本批管线互相印证。
6. **限定**：不称过渡态/极小值；不据此宣布任何方向的 Hessian"已验证"；max|g|≤1×10⁻⁵ 驻点验收门限保留原样。

## 5. 剩余问题与停止

**未解决**：k_H 与有限差分曲率的 ~3 倍量级矛盾中，网格响应解释 24.9%，剩余 75.1%（~1.04×10⁻⁴ Eh/Bohr²）集中在 DFT-XC 侧，来源待查（H3/H2 或其他）。**本批到此停止**——后续（RSH Hessian 方法学核查、软模子空间 Hessian 精度专项、或转向其他路线）待总指挥决定。
