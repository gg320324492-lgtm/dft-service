# NH3/O3 气相单体频率与稳定性核查批 · 频率报告（JOB-2026-0906-026）

**日期**：2026-09-08。**新增量化求值数**：SCF+梯度 **2**（每单体 1 次）；稳定性分析 **2**；解析 Hessian **2**；D2 纯函数差分核对 **4**（不计 SCF 求值）。
**方法**（从实际对象读回）：wb97xd（libxc）+项目 −D2 全导数 / def2-TZVP / DFT L8 / grid_response=True / SCF 1e-12,1e-9，气相。

## 1. 端点确认（`final_endpoint_index.json` 默认端点）

| 单体 | 端点 | E（Eh） | max\|g\|（Eh/Bohr） | 坐标与索引一致 | 实际传入坐标哈希 |
|---|---|---|---|---|---|
| NH₃ | NH3_gas_v5_final | −56.564219135911 | 7.661×10⁻⁷ | ✓（<1e-10 Å） | `7c02c6ca816c716d` |
| O₃ | O3_gas_final | −225.433902699575 | 3.514×10⁻⁶ | ✓ | `6675bc7b5bf2aaf4` |

- 原子顺序（NH₃: N,H,H,H；O₃: O,O,O）、电荷 0、自旋 0、单位 Å 均已核对；实际传入坐标已保存（`freq_check_results.json.endpoint_check.coords_passed_to_object`），未与格式化前坐标混写。
- Hessian 网格无静默回退：Hessian 前后 `mf.grids.level` 均为 **8**。

## 2. 内部稳定性（return_status=True，显式布尔）

| 单体 | stable_i | stable_e | 耗时 |
|---|---|---|---|
| NH₃ | **True** | None（未请求外部检查） | 3.6 s |
| O₃ | **True** | None（未请求外部检查） | 12.6 s |

- 调用 `mf.stability(return_status=True)`，按实际返回结构保存布尔值；原始日志：`freq_check/NH3_stdout.log`、`O3_stdout.log`（verbose=4）。
- **范围保留**：内部稳定通过 ≠ 排除多参考问题（O₃ 多参考特性、S13 R 行 T1=0.0346 为组合反应物值）；外部稳定性搜索、失稳解跟随、UKS 切换均未做（未授权）。

## 3. 分项 Hessian 与数值核对

| 项 | NH₃ | O₃ |
|---|---|---|
| 合成 Hessian（解析 DFT + 中心差分 D2）反对称残差（max\|H−Hᵀ\|） | 9.94×10⁻⁷ | 1.54×10⁻⁵ |
| D2 差分步长核对（默认 1e-3 vs 半步 5e-4，Bohr；矩阵最大差） | 5.26×10⁻¹³ | 4.06×10⁻¹³ |
| 组成 | 合成 = 解析 DFT + D2（解析 D2 梯度的中心差分，`d2_full.d2_hess`，步长 Bohr）；DFT 部分 = 合成 − D2；**不称"全解析"**；未重复加入 D2 | 同左 |
| 矩阵布局 | PySCF (n,n,3,3) → `transpose(0,2,1,3).reshape(3n,3n)`（022 错排教训的实现已核对） | 同左 |
| 原始矩阵保存 | `freq_check/{NH3,O3}_hess_{combined,d2,dft}.npy`（合成后先存原始值，再另存对称化；未覆盖） | 同左 |

响应求解：RKS Hessian 响应在 `hobj.kernel()` 内求解；显式收敛标志未由 API 暴露——**verbose=4 原始日志已保存**（`{sid}_stdout.log`，可查 "converged" 字样），并在结果 JSON 中注明该限制。

## 4. 内部模式与频率（质量 = `mol.atom_mass_list()` 丰度最高同位素；平/转投影 = 质量加权标准 TR 基）

### NH₃（3N−6 = 6 个内部模式；实测 6）
| # | 频率 (cm⁻¹) |
|---|---|
| 1 | 1024.4 |
| 2 | 1680.7 |
| 3 | 1681.2 |
| 4 | 3531.7 |
| 5 | 3659.4 |
| 6 | 3659.4 |

TR 基秩 = 6；正交性最大偏差 = 1.1×10⁻¹⁵；投影维度 = 12−6 = 6；**负模 = 无**。

### O₃（3N−6 = 3 个内部模式；实测 3）
| # | 频率 (cm⁻¹) |
|---|---|
| 1 | 784.5 |
| 2 | 1328.7 |
| 3 | 1340.3 |

TR 基秩 = 6；正交性最大偏差 = 6.7×10⁻¹⁶；投影维度 = 9−6 = 3；**负模 = 无**。

**PySCF 后处理核对**（同一矩阵、同质量、`imaginary_freq=False`——负频保留为负实数）：NH₃ 最大差 **3.6×10⁻⁶ cm⁻¹**、O₃ **1.3×10⁻⁶ cm⁻¹**——仅验证矩阵处理与质量约定，**不是独立电子结构验证**。本实现转换与 PySCF `transpose(0,2,1,3)` 一致（022 错排防范落实）。

## 5. 判定

| 单体 | SCF+梯度 | 内部稳定性 | Hessian/响应 | 内部曲率 | 判定 |
|---|---|---|---|---|---|
| NH₃ | ✓（7.661×10⁻⁷） | **stable_i=True** | ✓（反对称残差小、D2 步长核对通过、网格无回退） | **全正** | **在本批方法和检查范围内支持的局部极小值** |
| O₃ | ✓（3.514×10⁻⁶） | **stable_i=True** | ✓ | **全正** | **在本批方法和检查范围内支持的局部极小值** |

- 判定范围限定：**本批方法（wb97xd+−D2/def2-TZVP/L8，气相）和检查范围（内稳定 + 谐振频率）内**；不排除多参考（O₃）、不排除更高阶效应。
- 无负模、无未解决数值异常——**未触发位移跟随或重新优化**。
- 本批不生成焓、自由能、反应速率或结合能；气相结果**不解释为养殖水处理机制**。
- 与历史基线频率（0.0 阶段，grid5/D2 单点口径）**不直接比较**（方法与配置不同——精度一致性登记在案）。

## 6. 预算与操作计数

| 操作 | 实际 | 上限 |
|---|---|---|
| 新建对象端点 SCF+完整梯度 | **2** | 2 |
| 内部稳定性分析（return_status=True） | **2** | 2 |
| DFT 解析 Hessian | **2** | 2 |
| D2 纯函数差分核对（默认+半步 ×2 单体） | **4** | 另列，不计 SCF 求值 |
| 矩阵后处理/频率分析 | 若干 | 另列 |

## 7. 还需要什么

- **加合物初猜任务**：依据实际验收状态安排（两单体参考 + 参考能之和 −281.998121836 Eh 已就绪）；**加合物建模前仍须视觉核对 S13 Figure 1 结构图坐标**——不在本批自动启动；
- 水体类型/pH/波长待补充（不阻塞气相加合物工作）。
