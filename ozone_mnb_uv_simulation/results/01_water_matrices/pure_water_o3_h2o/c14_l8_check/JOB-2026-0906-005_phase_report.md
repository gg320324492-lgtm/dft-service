# JOB-2026-0906-005：频率交叉核对修订与 c14_plus L8 驻点核查批 · 总报告

## 正在做什么 / 已完成什么 / 还需要什么

- **在做什么**：阶段 A 修复频率交叉核对缺陷并补正 004 批记录（零 SCF）；阶段 B 仅对 c14_plus 执行一次 L8 优化并独立复核；阶段 C 对新 L8 端点做完整双步长内部频率分析与最低模核查。c06_plus 不重复优化、不重复完整 Hessian。
- **已完成**：A 通过（回归 12/12，同矩阵交叉核对两步长均达机器精度，4 项记录补正）；B 通过（45/60 步收敛，独立复核 max|g| = 2.541×10⁻⁶，结构保持 RMSD 0.020 Å）；C 通过（L8 端点 12/12 内部模式在两步长下全正、交叉核对 ~10⁻¹¹ cm⁻¹、最低模方向曲率全正且三套一致）。
- **还需要什么**：c14_plus 现可标注为"**在 L7/L8 已实施检查范围内支持的局部极小值**"（仍非全局最低、不称无限网格极限已收敛）；c06_plus 维持 004 批判定（L7 证据支持，其 L8 全谱未查、不在本批授权内）。后续批次再决定水合结构保留、水相更新与高层级对照。

**阶段状态**：A = **通过**；B = **通过**；C = **通过**。

---

## 1. 阶段 A：离线修复与报告补正（通过，零 SCF）

### 1.1 step3 脚本修复（`endpoint_freq_step3_frequency.py`）

- **缺陷**：004 版 `main()` 仅从 h=0.002 的 Hsym 构造 PySCF 布局矩阵并传给两个步长的 `analyse()`——h=0.004 的"交叉核对"实际比对的是 h=0.002 的矩阵。
- **数值证据**：h=0.004 交叉核对偏差与步长间 max|Δω| **精确相等**（c14: 0.0834 vs 0.0834；c06: 0.7675 vs 0.7675，ratio = 1.000000）。
- **修复**：每步长用**自身** Hsym 构造 PySCF 布局矩阵；加**同矩阵往返断言**（(p,q,x,y)→(p,x,q,y)→18×18 与 Hsym 逐一相符，atol 1e-12）；保留测试钩子 `override_hess_pqxy` 复现旧错误路径。
- **回归测试**（`endpoint_freq_step3_revision.py` → `step3_revision_regression.json`，**12/12 通过**）：T1 往返断言成立；T2 自身矩阵交叉核对 h=0.002 = 2.7×10⁻¹¹/9.5×10⁻¹¹、h=0.004 = **4.5×10⁻¹¹/1.1×10⁻¹⁰ cm⁻¹**（均机器精度）；T3 误传 h=0.002 矩阵时偏差变大（0.0834/0.7675）且 `same_matrix_as_eigenproblem=False` 被标记——即修复前行为会被本测试大声捕获。
- **撤回**：~~"h=0.004 差异来自独立对角化舍入"~~。真实同矩阵频率差（两步长各自与 PySCF 之差）已分别保存于 `frequency_analysis_revised.json`。
- 重分析全部使用已存 144 点原始梯度，**未重跑任何历史 SCF**。

### 1.2 矩阵比较名称更正（三分离）

| 比较（原名称 → 更正） | c14_plus | c06_plus |
|---|---|---|
| ~~internal_matrix_max_diff~~ → **Cartesian Hessian 差异** max\|H_sym(h1)−H_sym(h2)\| | 1.25×10⁻⁵ | 9.63×10⁻⁶ |
| **同一 U 基底下质量加权内部矩阵差异** max\|Hint(h1)−Hint(h2)\| | 9.75×10⁻⁶ | 3.25×10⁻⁶ |
| **模式重叠匹配**最小重叠 / max\|Δω\| | 0.999995 / 0.0834 cm⁻¹ | 0.992443 / 0.7675 cm⁻¹ |

反对称残差不再以"比对角元小"单独判定：与步长趋势（h 加倍增大 ~4 倍——有限差分截断特征）、质量加权内部尺度（Hint Frobenius 范数 ~1.1）及最低模式变化（0.08/0.77 cm⁻¹）一并呈现；**以上比较均不冒充严格误差上界**。

### 1.3 实际任务数量核对（`softmode_raw.json` 逐条统计）

| 项 | 实际执行 | 授权上限 |
|---|---|---|
| 唯一位移点（grid, amp, sign） | **16**（每候选 8） | 32 |
| 方向数 | 每候选 1（最低模） | 每候选 ≤2 |
| 新增中心点 | 2（两候选 L8 各一；L7 中心复用 004 步骤 1） | "必要中心点" |

上限值不再被写成执行数量。

### 1.4 结论修订（已同步至 004 报告/JSON、任务记录与项目记忆，保留取代关系）

- 两候选保留"L7 下双步长内部谱全正"结果。
- c14_plus：~~"无待定项"~~ → **"L7 局部极小值证据支持，网格敏感性待完成"**（待定项保留）；其 L8 非驻点处的正方向曲率仅刻画该位置该方向的局部曲率，不证明 L8 存在稳定极小值。
- c06_plus 网格鲁棒性仅覆盖实际核查的最低方向，不扩展为全部 L8 内部谱。
- 最低方向曲率 L7/L8 比值由数据生成：**c14 k_E 比 4.029/3.690（小/大幅度）、k_g 比 3.910；c06 k_E 比 0.995/0.994、k_g 比 0.994**（不再用"约三倍"）。

## 2. 阶段 B：c14_plus 一次 L8 优化（通过）

- 起点：已验收的 L7 attempt2 端点（`endpoints_fixed.json`），非旧搜索结构。
- 方法：与已验证路径完全一致，**仅网格改 L8**——气相、xc='wb97xd' + 项目经验色散全导数（参数逐字不变）、def2-TZVP、grid_response=True（grad_factory 强制）、SCF 1e-12/1e-9；优化器 pyberny 0.7.0 内坐标阈值 gradientmax=gradientrms=1e-6（上批核实）、maxsteps=60。**仅一次运行**，未重启/延长/改阈值。
- 逐步**完整 6×3 梯度**即时保存（004 缺项已闭合）：45 步全部 SCF 收敛、grid_response=True、grid=L8、无非有限值（`l8_optimize_result.json` 含完整轨迹）。
- 收敛：第 **45/60** 步优化器全判据满足（未触上限）。
- **独立新建 L8 对象复核**：E = −301.874837967 Eh，**max|g| = 2.541×10⁻⁶ ≤ 1e-5 ✓**，rms 1.136×10⁻⁶，SCF 收敛且有限，dE vs 优化器末步 = −4.6×10⁻¹³（机器精度）；端点 D2 = 解析值（恰一次）。
- 能量变化：起点 → 终点仅 −1.82×10⁻⁷ Eh（L7 端点在 L8 下本已近驻点）。
- **正式配准映射**（`formal_mapping.json`，Kabsch + 已核实置换集）：
  - L7 端点 ↔ L8 端点：**RMSD = 0.0204 Å**（无置换）→ **结构保持，未发生构型转换**；
  - L8 端点 ↔ c06_plus：RMSD = 3.7427 Å → 仍为不同构型。

## 3. 阶段 C：新 L8 端点完整频率与最低模核查（通过）

### 3.1 双步长差分 Hessian（72 点，L8）

18 方向 × ± × h∈{0.002, 0.004} Bohr；每点完整梯度/能量/实际坐标/指纹即时保存；72/72 全部 SCF 收敛且被接受（0 失败、0 跳过），位移核验 <10⁻¹⁰ Bohr。未对称化 Hessian 已保存：反对称残差 max = 2.09×10⁻⁶（h=0.002）/ 8.37×10⁻⁶（h=0.004）Eh/Bohr²（同 §1.2 的呈现方式），随后才对称化。

### 3.2 完整内部谱（L8 端点，cm⁻¹，符号保留）

| # | h=0.002 | h=0.004 | | # | h=0.002 | h=0.004 |
|---|---|---|---|---|---|---|
| 1 | 38.2 | 38.5 | | 7 | 788.2 | 788.2 |
| 2 | 69.7 | 69.8 | | 8 | 1332.0 | 1332.0 |
| 3 | 83.1 | 83.1 | | 9 | 1341.8 | 1341.8 |
| 4 | 93.9 | 93.9 | | 10 | 1631.3 | 1631.3 |
| 5 | 171.0 | 171.0 | | 11 | 3870.3 | 3870.3 |
| 6 | 247.5 | 247.6 | | 12 | 3976.1 | 3976.1 |
| **负模数** | **0** | **0** | | | | |

- **每步长使用自身矩阵**的同矩阵 PySCF 交叉核对：2.48×10⁻¹¹ / 1.28×10⁻¹¹ cm⁻¹（机器精度）。
- 两步长比较（三分离）：Cartesian 差异 1.22×10⁻⁵、同 U 内部矩阵差异 6.44×10⁻⁶、最小重叠 0.999974、max|Δω| = 0.29 cm⁻¹。
- 负本征值/虚频保留机制就位（本批无负模）。

### 3.3 最低模独立方向核查（L8，4 点）

| 幅度 | k_E | k_g | Hessian 投影 pᵀHp | 斜率校验残差 |
|---|---|---|---|---|
| ±0.002 Å | +6.018×10⁻⁵ | +6.065×10⁻⁵ | **+5.970×10⁻⁵** | 2×10⁻⁹ |
| ±0.004 Å | +6.194×10⁻⁵ | +6.441×10⁻⁵ | （同上） | 8×10⁻⁹ |

全正、三套一致（1–8%，含 O(q²) 漂移）；L8 中心即 Phase B 独立复核驻点（max|g| = 2.541×10⁻⁶，满足驻点条件）。
**注（JOB-2026-0906-006 修订）**：在 L8 **驻点**处 L7/L8 曲率比降为 ~1.18（k_E 比 = 7.089/6.018），远小于 004 批在 L8 非驻点处测得的 4.03。~~证实当时的量级差异主要来自比较位置非驻点（L8 梯度未消），而非纯网格曲率敏感性~~ → **准确表述**：重新优化后，各自最低方向的曲率差异减小；重优化同时改变了几何与模式方向，两者贡献尚未单独分解，不将非驻点位置单独指认为主要根因。

## 4. 判定

| 问题 | 结论 |
|---|---|
| L8 驻点是否收敛 | **是**（45/60 步；独立复核 max\|g\|=2.541×10⁻⁶，全判据满足） |
| 新结构是否保持原构型 | **是**（正式配准 RMSD = 0.0204 Å） |
| 12 个内部模式是否全部为正 | **是**（L8 双步长均 12/12 正；负本征值/虚频保留机制在位） |
| 双步长与最低方向证据是否一致 | **是**（Δω ≤ 0.29 cm⁻¹、重叠 ≥0.999974；最低模三套曲率一致为正） |

**c14_plus 判定更新**：~~"L7 局部极小值证据支持，网格敏感性待完成"~~ → **"在 L7/L8 已实施检查范围内支持的局部极小值"**。仍不称全局最低，不称无限网格极限已收敛。
**c06_plus 维持 004 批判定**：L7 证据支持；其 L8 全谱未查（不在本批授权内），网格鲁棒性结论仅限已查最低方向。
无负模 → 无鞍点候选；无归零、无删模、无方法调整。本批未运行 L9、未计算 c06 新完整频率、未做 SMD/高层级单点/热化学/结合自由能/反应路径。

## 5. 文件指纹与取代关系

| 文件 | SHA-256（前 16 位） |
|---|---|
| protocols/endpoint_freq_step3_regression.py | 7ddb583ca5e60252 |
| protocols/endpoint_l8_optimize.py | d451d30e2d9a0811 |
| protocols/endpoint_freq_step2b_hessian_l8.py | 489f4a8aba289c1a |
| protocols/endpoint_freq_step3b_frequency_l8.py | 7d390f419d9b7257 |
| protocols/endpoint_freq_step4b_softmode_l8.py | 0451bb4f1f5cabf6 |
| run_artifacts/.../c14_l8_check/l8_optimize_result.json | 29ecc3617147fefb |
| run_artifacts/.../c14_l8_check/formal_mapping.json | 9c079fc3daab44f1 |
| run_artifacts/.../endpoint_frequency/frequency_analysis_l8_c14.json | 9333a0d95fb47ac8 |
| run_artifacts/.../endpoint_frequency/softmode_l8_c14.json | 54332998d84f0e86 |
| run_artifacts/.../endpoint_frequency/frequency_analysis_revised.json | a4e10cf9a94d3f78 |
| run_artifacts/.../endpoint_frequency/step3_revision_regression.json | 45d10a3b0ebe5b67 |

取代关系：`frequency_analysis_revised.json` 取代 004 `frequency_analysis.json` 的交叉核对字段（原文件保留）；本报告与 `endpoint_frequency_summary.json` 的修订块取代 004 报告/JSON 中对应结论；L8 轨迹 `l8_optimize_result.json`（含 45 步完整梯度）为 c14_plus L8 端点的正式记录。历史原始文件未覆盖。

本批停止。下一批再根据结果决定水合结构保留、水相更新及高层级对照安排。
