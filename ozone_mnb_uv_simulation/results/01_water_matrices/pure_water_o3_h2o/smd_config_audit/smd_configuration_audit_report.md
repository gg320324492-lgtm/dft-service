# JOB-2026-0906-007：SMD 同配置验证修复与未收敛轨迹诊断批 · 审计报告

## 本批做什么 / 已完成什么 / 还缺什么

- **在做什么**：复现并修复 006 批阶段 B 的网格配置错误（生产 L7 vs 原生 L8）；以同配置（双侧 L8）重做独立验证；复核两个水相终点的 max|g| 记录；离线诊断 60/40 步轨迹并给出每候选一个有证据的后续方案。不追加优化、不放宽阈值、不计算频率。
- **已完成**：配置错误精确复现并修复（双侧显式 L8 + 计算前逐项配置断言）；无 SCF 配置回归 **5/5 通过**（单侧 L7 在计算前被捕获）；同配置五点验证 **overall_pass=True**（全部残差降至机器精度）；两终点对照确认 **max|g| 记录精确成立**；轨迹诊断与后续方案完成；006 报告/JSON/notes 的错误归因已撤回修订。
- **还缺什么**：两候选水相驻点仍未收敛（本批不执行优化）；c06 的 2.58×10⁻⁵ 残差性质（真实力 vs 溶剂表面数值噪声）待导数诊断判定；内坐标判据实际数值无日志（记 missing）。

**阶段状态**：A = **通过**；B = **通过**；C = **通过**（离线诊断，方案仅提出不执行）。

---

## 1. 阶段 A：配置错误复现与修复（通过）

### 1.1 复现（逐项核实）

| 项 | 实际情况 |
|---|---|
| `smd_restart_step2_verify.py` 生产分支 | `make_mf_d2_gr(mol, solvent='water', grid_response=…)` **未传 grid_level** |
| `grad_factory.make_mf_d2_gr` 默认值 | `grid_level=GRID_LEVEL = 7` |
| 同脚本原生基准分支 | `mf.grids.level = GRID_LEVEL = 8`（显式） |
| `phaseB_smd_verification.json` 方法指纹 | **grid_level = 7**（读自实际对象） |
| 阶段 C 优化与独立复核 | 显式 L8（两侧一致） |

### 1.2 受影响范围

- **保留（附正确配置标签）**：scanner-vs-fresh 可复现性（实为 **L7-vs-L7**）；同密度 D2-once 机器精度（实为 **L7-vs-L7**）；grid_response 活跃性检查（实为 **L8-vs-L8** native 内部）。
- **撤回**："同配置独立 SMD 基准通过"（实为 **L7-vs-L8 跨网格**）、"L8 五点验证完成"（生产侧为 L7）。
- **撤回**：~~"独立原生基准残差属于网格响应/跨路径噪声"~~——无依据的归因。同配置重跑证明该梯度残差（~3–4.6×10⁻⁵）就是 **L7/L8 跨网格差异**（同配置后降至 ~10⁻¹²，见 §2）。
- **阶段 C 数据**：显式 L8（优化与复核一致）——不因此失效，也不视为已充分证明；其 max|g| 记录经本批终点对照精确复现（§3）。

### 1.3 脚本修复与配置回归

- `smd_config_audit_verify.py`：双侧显式 `grid_level=8`；**计算前**逐项配置断言（xc、基组、电荷/spin、DFT 网格级别、SCF 阈值、max_cycle、溶剂、腔体/表面离散参数 lebedev_order/vdw_scale/r_probe）——**全部指纹读自实际对象**，不以脚本常量冒充；DFT 积分网格与溶剂表面网格分开记录（`dft_grid_level` vs `lebedev_order`/`solvent_surface_points`）；`d2_attached` 与默认 `grid_response` 旗标为预期差异，单列记录不比较。
- 无 SCF 配置回归（`test_smd_config_regression.py`，**5/5 通过**）：T1 L8/L8 断言通过；T2 **故意单侧 L7 → 计算前 CONFIG MISMATCH 失败**（捕获 `dft_grid_level 7≠8`）；T3/T4 指纹读自实际对象（L7 对象报 7、L8 对象报 8）。未修改任何全局默认值。

## 2. 阶段 B：同配置独立验证（通过，L8 双侧）

容差预先固定（与 006 相同，未放宽）：scanner-vs-fresh |dE|<1e-6、max|dG|<1e-5；D2-once < max(1e-2×|D2|, 1e-6)；A→A <1e-8。

| 检查 | 结果 |
|---|---|
| **配置一致**（计算前逐项断言） | **通过**（xc/basis/charge/spin/DFT 网格/SCF 阈值/溶剂/腔体参数全同；旗标单列） |
| scanner vs 独立新建生产对象（5 几何） | **通过**：dE ≈ 10⁻¹³、max\|dG\| ≈ 10⁻¹³–4×10⁻¹⁰ |
| 全新原生 SMD（L8）+ 显式项目色散 vs 生产 | **通过（机器精度）**：dE ≈ 10⁻¹³、max\|dG\| ≈ 10⁻¹²–4×10⁻¹⁰（006 的 3–4.6×10⁻⁵ 残差消失——证实为跨网格差异） |
| 同密度 D2 恰好计一次（5 几何） | **通过**：resid_E ≈ 10⁻¹⁴、resid_G ≈ 10⁻¹⁵ |
| A→B→A 返回一致性 | **通过**：dE = 1.25×10⁻¹²、max\|dG\| = 5.25×10⁻¹⁰ |
| grid_response 执行 | **通过**（路径+配置核查：所有 scanner 调用旗标 True、生产对象梯度工厂强制 True；True/False 数值差 2.76×10⁻⁵ 作旁证记录，不作普适门） |
| 能量差分斜率 vs 完整响应梯度 | s_E = −1.333×10⁻³ = d·g₀（残差 −9.7×10⁻⁹/−3.9×10⁻⁸） |
| 两步长曲率趋势 | k_E +1.905/+1.903×10⁻³、k_g +1.904/+1.900×10⁻³（一致） |

缺失值：无（所有检查均有实测值）。若独立基准不一致，本批预案为分解配置/密度/分量/梯度来源——未触发。

### 两水相终点对照（同配置 L8）

| 终点 | 本次测得 max\|g\| | 006 记录 | **记录成立** | 同密度 D2-once 残差 |
|---|---|---|---|---|
| c14_plus（60 步未收敛几何） | 2.3867×10⁻³ | 2.3867×10⁻³ | **True** | E 1.2×10⁻¹³ / g 1.8×10⁻¹⁵ |
| c06_plus（40 步停止几何） | 2.5803×10⁻⁵ | 2.5803×10⁻⁵ | **True** | E 9.1×10⁻¹⁴ / g 4.4×10⁻¹⁶ |

原始终点最大梯度记录**成立**（同配置 L8 重测逐位一致）。

## 3. 阶段 C：未收敛轨迹诊断（离线，无新 SCF）

### c14_plus（60 步，触上限）

- **轨迹结构**：前 26 步单调弛豫（梯度 4.3×10⁻² → **8.34×10⁻⁵**，step 26 为最低梯度步，间隙 3.51 Å）；**step 30 发生大步长结构重排**（相邻位移 0.055–0.079 Å，为全程最大），梯度跳回 2.7×10⁻³；其后能量继续单调下降（最低能量步 = 末步 60，E = −301.891478）但梯度维持 ~10⁻³。
- **分类**：单调弛豫 + **一次大步长结构重排** + 截断；能量上升 3 次（最大 +4.26×10⁻³）、显著位移中符号变化 6 次——重排事件伴随；非纯振荡，非异常步。
- **梯度回升对应的结构运动**：step 26→30 间水分子绕 O3c 的大幅重取向/平移（位移 0.055–0.079 Å，gap 保持 3.51 Å——分子内重排而非解离方向）。

### c06_plus（40 步，优化器停止）

- **真实停止原因**：`optimizer_converged=True`——**pyberny 内坐标判据满足的正常收敛停止**（非异常退出、非步长失败）；判据实际数值**无日志，记 missing**（不由"成功"标志反推）。
- **Cartesian 残差特征**：step 36–40 梯度平台 2.55–2.71×10⁻⁵、相邻位移 ≈0（0.000–0.001 Å）——内坐标已收敛而 Cartesian max|g| 停在 2.58×10⁻⁵。其性质（真实残余力 vs SMD 溶剂表面离散噪声）**无法由现有数据判定**——需导数诊断（§4）。

### 后续方案（仅提出，不执行；不讨论放宽 max|g|≤1e-5）

| 候选 | 方案 | 起点与理由 | 拟议步数上限 |
|---|---|---|---|
| **c14_plus** | **继续优化 + 调整优化器设置**：从保存的 **step-60 几何**（最低能量 −301.891478，弛豫仍在单调进行）重启，**减小 berny 初始 trust radius（默认 0.3 → 0.1）**以避免 step-30 型大步长重排再次重置进度 | step-60 几何；能量单调下降且梯度回升源于单次大位移重排，小步长下 step-26 已证明梯度可达 8×10⁻⁵ 量级 | 60 |
| **c06_plus** | **先做具体导数诊断**：沿 Cartesian 最大梯度分量做小规模有限差分（已验证 L8 路径，数个点）判定 2.58×10⁻⁵ 残差是真实力还是溶剂表面离散噪声；若为真实力 → 从 **step-40 几何**继续（内坐标已收敛、位移≈0，重启代价最小），cap 30；若为噪声底 → 提交用户作阈值讨论（不静默接受） | step-40 几何 | 诊断后定（≤30） |

## 4. 报告与包装说明补正（已落实）

1. **撤回**"独立原生基准残差属于网格响应/跨路径噪声"（无依据归因）→ 实为 **L7/L8 跨网格差异**（同配置重跑实测：残差 ~3–4.6×10⁻⁵ → ~10⁻¹²）。
2. **undo_solvent**：实测发生的只有**断言失败**（可复现）；"D2 双重计入"为**推测的潜在后果，从未被观测**，未出现在任何已保存结果中——notes 已改写。
3. **CDS**："解析"仅指 Python 层以成对能量/梯度接口调用 C 例程 `mnsol_interface_`；**C 内部算法未核验**——notes 与报告已加限定。
4. 原始文件保留（`phaseB_smd_verification.json` 等未动）；新批数据另存 `run_artifacts/.../smd_config_audit/`；006 报告/JSON 顶部加修订块，任务与记忆同步。

## 5. 文件指纹

| 文件 | SHA-256（前 16 位） |
|---|---|
| protocols/smd_config_audit_verify.py | （见 JSON artifacts） |
| protocols/test_smd_config_regression.py | （见 JSON artifacts） |
| protocols/smd_config_audit_endpoints.py | （见 JSON artifacts） |
| protocols/smd_config_audit_trajectory.py | （见 JSON artifacts） |
| run_artifacts/.../smd_config_audit/phaseB2_same_config_verification.json | （见 JSON artifacts） |
| run_artifacts/.../smd_config_audit/endpoint_recheck.json | （见 JSON artifacts） |
| run_artifacts/.../smd_config_audit/trajectory_diagnosis.json | （见 JSON artifacts） |
| run_artifacts/.../smd_config_audit/config_regression.json | （见 JSON artifacts） |

## 6. 本批明确回答

1. **配置错误影响**：006 阶段 B 生产侧 L7 / 原生侧 L8 跨网格；"同配置独立基准通过"与"L8 五点验证"撤回；scanner 复现与同密度 D2-once（L7-vs-L7）及 gr 活跃性（L8-vs-L8）保留但换标签；阶段 C（显式 L8）不失效、其 max|g| 记录经重测精确成立。
2. **同配置验证**：全部通过（§2），此前门槛未放宽；无 missing 项。
3. **终点梯度记录**：两终点均精确成立。
4. **后续方案**：c14 = step-60 几何重启 + trust 0.1，cap 60；c06 = 先导数诊断判定残差性质，再定（真实力 → step-40 继续 cap 30；噪声底 → 用户阈值讨论）。仅提出，未执行。

本批停止。验证已闭合；下一批再安排有限步数的水相优化；驻点通过后才计算水相频率。
