# JOB-2026-0906-013：c06 order47 新端点诊断性内部频率批 · 报告

## 本批在做什么 / 已完成什么 / 还需要什么

- **在做什么**：在 011 批 c06 order47 **新端点**（正式索引核验，禁用旧 step-40 几何）上，以明确坐标/投影/数值约定做**诊断性内部曲率**计算（72 点双步长差分 Hessian）+ 最多两个方向的同几何 order41/47 独立核查。**授权范围=诊断计算；正式驻点验收标准不变。**
- **已完成**：中心复现（容差运行前固定，通过）；72/72 位移点（逐点配置与位移核验、即时保存）；未对称化→反对称残差→对称化流程；质量加权/Eckart 型投影/三块分解/12 模谱（负值完整保留）；PySCF 同质量交叉核对（矩阵处理已验证）；双步长模式匹配；方向核查 8 点 + 1 个 order41 中心。**实际求值 82/90**（1 中心 + 72 + 1 中心 + 8 方向），未扩量。
- **还需要**：软模曲率符号的跨档位矛盾（47 负 / 41 正）需用户裁决是否立项做档位收敛性刻画（见结尾建议）；正式驻点验收与频率/热化学仍被阻塞。

---

## 1. 输入核验与中心复现

- 端点来源链：正式索引 `endpoint_index.json::c06_order47_reopt` → 原始来源 `smd_order47_opt/order47_optimization.json::endpoint_coords_angstrom`；坐标 SHA（全精度）`bf912a791b3e9761` 与索引一致；原子顺序 O,O,O,O,H,H（项目约定）；完整梯度取自 011 独立复核。**未使用** step-40 旧几何。
- 中心复现（order47/L8 全配置重建）：ΔE=**5.7×10⁻¹⁴** Eh、Δg=**1.8×10⁻¹²** Eh/Bohr——容差（运行前固定为 1e-9 / 1e-8）内通过。
- 中心 max|g|=**2.5034×10⁻⁵** > 1e-5：本批为已授权的非完整驻点诊断，**验收状态不变（未通过）**。
- 指纹全部从实际对象读取；DFT 网格（L8，520384 点）与溶剂表面（order47，2700–2753 点随取向波动）**分项记录**。

## 2. 双步长差分 Hessian（72 点）

- 18 个直角坐标方向 × h=0.002/0.004 Bohr × ±；逐点核对实际位移（≤10⁻⁸ Bohr）与实际设置（order47/L8 前后读回）；SCF 全收敛、全有限；**无失败点、无重试**。
- **未对称化 Hessian 先行保存**（`hessian_batch.json` 内 raw 与 sym 分列，原始永不被覆盖）；反对称残差：h=0.002 max|H−Hᵀ|=**3.07×10⁻⁶**（相对 5.4×10⁻⁶）；h=0.004 **1.13×10⁻⁵**（相对 2.0×10⁻⁵）——FD 噪声量级正常，对称化不掩盖任何异常。

## 3. 内部曲率与外部耦合（约定成文）

约定：质量 amu=[15.9949×4, 1.0078×2]；H_mw[i,j]=H_sym[i,j]/√(mᵢmⱼ)；外基=质量加权平动+绕质心转动（QR 正交，**外秩 6**）；内补 U 形状 **18×12**（正交性误差 ~1e-15 量级）。

| 量 | h=0.002 | h=0.004 |
|---|---|---|
| 耦合块 QᵀH_mwU 的 RMS | 2.446×10⁻⁵ | 2.444×10⁻⁵ |
| 梯度质量加权范数：内部 / 外部 | 1.550×10⁻⁵ / 1.385×10⁻⁵ | 同左（同一中心） |

**内部 12 模（cm⁻¹，负值=虚频，完整保留）**：
- h=0.002：**−52.3**, 50.8, 66.1, 97.7, 203.7, 319.3, 790.4, 1265.2, 1351.9, 1603.9, 3790.4, 3907.4
- h=0.004：**−52.9**, 50.7, 66.0, 97.7, 203.5, 319.2, 790.4, 1265.2, 1351.9, 1603.9, 3790.4, 3907.5

**PySCF 交叉核对**（同质量 `mol.mass`、其自身 TR 投影，仅验证矩阵处理）：谱最大差 **0.35 cm⁻¹**、虚模数一致（1 个）。
> 过程记录（如实）：初版交叉核对曾出现"7 个虚模"假象，根因是**我方**将 18×18 矩阵 `reshape(6,6,3,3)` 错排后传入 PySCF（reshape 重排平坦缓冲而非行列分解）；改为 `reshape(6,3,6,3).transpose(0,2,1,3)` 后完全吻合。PySCF 侧无问题；该错排不影响本批主分析（主分析直接使用 18×18 矩阵）。

**双步长模式匹配**：逐模重叠 ≥**1.0000**（4 位小数），最大 |Δν|=**0.62 cm⁻¹**，无近简并标志；双步长一致**不构成严格误差上界**（如实声明）。

## 4. 方向核查（同几何 order41/47；规则先于求值固定）

选向规则（固定于任何方向求值之前，记录于 `analysis.json`）：主矩阵 h=0.002；dir1=最低内模；dir2=最负模（若有 λ<0）或最大双步长频移模（阈值 20 cm⁻¹）。结果：最低模=最负模（同为模 0）→ **去重为单一方向**（规则意图为第二个独立方向；节省 8 次重复求值）。

方向=模 0 的归一化 Cartesian 方向（最大原子位移向量长=1）；固定直线位移 ±0.002/±0.004 Å；**各档位用各自中心能量/梯度**；不重新优化、不旋转对齐。

| 档位 | 中心 max\|g\| | 投影梯度 (Eh/Å) | FD 斜率（吻合度） | 能量 FD 曲率 | 梯度差分曲率 | order47 Hessian 投影 |
|---|---|---|---|---|---|---|
| **47** | 2.50×10⁻⁵ | **+1.019×10⁻⁵** | +1.021×10⁻⁵（1.6×10⁻³） | **−4.248×10⁻⁴** | **−4.259×10⁻⁴** | −4.268×10⁻⁴ |
| **41** | 1.26×10⁻⁴ | **−1.118×10⁻⁵** | −1.118×10⁻⁵（2.0×10⁻⁴） | **+2.176×10⁻⁴** | **+2.216×10⁻⁴** | （上列；41 侧无本批 Hessian） |

- 三种估计（能量 FD、梯度差分、Hessian 投影）在 order47 相互核对、彼此吻合（≤5×10⁻³）——**负曲率证据自洽**。**【014 批补正】**"独立"改为"相互核对"：三者中梯度差分与 Hessian 投影共享同一 FD 导数实现（非完全独立），只有能量 FD 来自纯能量差分路径。
- **符号翻转**：同一几何、同一方向，order47 曲率为负、order41 为正（|量值|同数量级）；投影梯度符号亦随档位翻转。幅度间的量值变化（47：−4.248→−4.248；41：+2.176→+2.273）远小于档位间差异。

## 5. 判定（按批内三分类）

- **出现负曲率**：登记 **"负内部曲率诊断证据（JOB-013 指定约定：order47、直角坐标 FD、质量加权 Eckart 型投影）"**——一个软虚内模 −52.3 cm⁻¹（h=0.004 为 −52.9）。
- **符号翻转/数值敏感性影响解释**：该模式曲率符号在 order41 同几何**翻转为正** → "是否存在物理真实的内部软模"**标待定**；所缺证据=该方向曲率随表面档位的收敛行为。
- 不称过渡态；不跟随模式；即使部分谱为正**不得**进入热化学、结合自由能或反应结论。
- 非完整驻点处的总体警示：曲率解释依赖所定义位移路径；投影正谱不证明内部/完整极小值。

## 6. 实际任务计数与缓存声明

| 类别 | 实际 | 上限 |
|---|---|---|
| Hessian 位移点 | 72 | 72 |
| 方向核查位移点 | 8 | 16 |
| 中心点（47 复现 + 41 新中心） | 2 | 2 |
| **合计** | **82** | **90** |

本批全部求值均为新算并即时落盘；**无任何按文件名的缓存复用**（每次使用实际坐标+完整指纹核对）。

## 7. 文件与平台边界

新增：`run_artifacts/01_pure_water_o3_h2o/smd_diag_curvature/`（`center_order47/41.json`、`hessian_points.json`、`hessian_batch.json`（raw+sym）、`analysis.json`、`projection_matrices.json`（H_mw/Q_ext/U）、`direction_points.json`、`direction_checks.json`、日志×2）；脚本 `protocols/{smd_diag_hessian, smd_diag_analysis, smd_diag_directions}.py`；本报告与汇总 JSON 在 `results/.../smd_diag_curvature/`；任务记录 `jobs/JOB-2026-0906-013_diag_curvature_batch.md`。历史原始未覆盖；**未修改平台文件与 PySCF 安装，未进入平台修复流程**。

---

## 结尾三事项（按批令分列）

1. **正式驻点验收：仍未通过**（中心未投影 max|g|=2.5034×10⁻⁵ > 1e-5；本批未改变阈值、未删除外部分量）。
2. **本批诊断状态**：order47 约定下**负内部曲率诊断证据**（单一软虚内模 −52.3 cm⁻¹，三种估计相互核对一致——见第 4 节 014 批补正）；该模式曲率符号对表面档位敏感（41 为正）→ 模式物理实在性**待定**；辅助标签"内部优化条件满足"（内残差 1.31×10⁻⁷）维持登记。
3. **唯一建议的下一步及依据**：请用户裁决是否立项"**软模曲率的档位收敛性刻画批**"——在 order47 端点固定几何、同一方向与约定下补齐更细档位（先只读核实 order59 支持；每档 1 中心 + 4 位移点，新增 ≤10 次求值；41/47 已有 8 点直接复用），检验 dᵀHd 符号/量值随档位的趋势，判读规则事先成文——**【014 批补正：判读定位】**该核查只能回答"固定几何、固定方向上该曲率量的数值敏感性"问题，**不能单独证明完整驻点或物理极小值**；"随档位单调趋负"**不作为**收敛判据或真实性判据，仅作为档位敏感性是否减弱的观察记录。依据：本批显示该量对档位敏感且符号翻转，是判断"软模实在性"的**一个重要证据缺口**（非"唯一"）；模态跟随、热化学等路径在此基线问题解决前**不优先安排**（原"均无意义"表述过强，撤回）。
