# JOB-2026-0906-019：JOB-018 执行记录核实与第一阶段成果整理批

## 范围与授权
- 任务编号 = 018 之后下一个未占用（已核对）；**新增量化求值数 = 0**。
- 结束本轮 O₃···H₂O 自动追加诊断；核实 JOB-018 实际执行次数；整理第一阶段成果。

## 执行状态（全部完成）
| 项 | 交付 |
|---|---|
| 1. 执行次数核实 | `run_artifacts/.../paired_sp/execution_audit.json`（详见下） |
| 2. 驱动记录机制修正 | `protocols/sp_ledger.py`（账本模块）+ `paired_single_points.py` v2 + `test_sp_ledger.py`（无 SCF 检查 **3/3 通过**） |
| 3. 能量表与来源表述核对 | 018 报告/JSON/任务记录 4 组补正（全精度重算、D2 符号、哈希类型、逐位表述） |
| 4. 结束自动追加 + 阶段成果 | 0.2 kcal/mol 判据删除、几何选择敏感性暂缓、c14 L8 结果复用登记；`results/phase1_closeout/phase1_results_summary.md` |

## 1. 执行次数核实（JOB-018）

| 次数项 | 值 | 证据 |
|---|---|---|
| 脚本启动 | **3** | 会话记录（run1/run2 日志被 `>` 重定向覆盖）+ run3 日志（文件） |
| SCF 启动 / 返回 | **5 / 5** | run1：0（计算前脚本错误）；run2：1 返回（梯度在其后被调用为证；**是否收敛无法确定**——未保存）；run3：4/4（文件） |
| 梯度启动 / 返回 | **5 / 5** | 同上（run2 梯度返回后崩溃于表面属性读取行） |
| 成功保存唯一点 | **4** | `single_points.json`（run3，文件） |
| 计算后未保存（重复求值） | **1**（c06 气相） | run2 崩溃于保存前；该值丢失，无法与 run3 重算值比较（如实说明） |

**上限核算**：原上限 4 且"失败尝试计入"→ 实际尝试 **5，超限 1 次**。原因：脚本缺陷（表面/属性读取置于 SCF 与梯度**之后**）。影响：多耗 1 次气相求值机时；无科学结果影响（未保存值不可比，已如实标注）。原"无失败、4/4"表述已在 018 报告/JSON/任务记录中按此更正。

**区分**：run1 = 计算前脚本错误；run2 = 计算后记录错误（SCF 收敛性无法确定）；run3 = 正常完成。无 SCF 失败情形。

## 2. 驱动记录机制修正 + 无 SCF 检查（3/3 通过）

- `sp_ledger.py`：attempt 在**计算前**持久化；SCF/梯度/保存状态分项记录；**重启读取已有账本、预算不重置**；成功结果按 (tag, 坐标哈希, 配置) 键保存并在重启时核验复用；错误记录**只追加不覆盖**。
- 配置/属性检查前移至计算之前；表面读取改为**保存后**的容错增强（读取失败不再丢弃已算结果——run2 缺陷根除）。
- 无 SCF 模拟检查：[A] 计算后读取错误 → 状态 error 且已算值记录在案、结果未保存，重启后预算保留并成功重跑；[B] 重启预算保留（3 次历史尝试 → budget_left=1）；[C] 同键结果复用不重算。**全部通过**。

## 3. 能量表与来源表述核对

- 能量差由**原始全精度结果**重算（展示可舍入，计算不用舍入值）；`unified_energies.json` 数值不变。
- **D2 列改为有符号贡献**（c06 −1.603×10⁻⁴、c14 −1.722×10⁻⁴ Eh），消除表头"−D2"与正数的混淆。
- **坐标哈希 vs 文件哈希**：分别只与 registry 内联坐标哈希 / registry source_hash 比较（同类型），不得互相换算。
- **"逐位复现"更正**：c14 气相 L8 单点与验收记录全精度差 E=9.66×10⁻¹³ Eh、max|g|=1.01×10⁻¹¹ Eh/Bohr——**非逐位**，显示精度下一致（SCF 再现水平）。
- 结果范围保留：固定气相几何、现有 SMD 配置下的电子能观察；不称溶剂化自由能或稳健水相排序。

## 4. 结束自动追加与后续入口

- **几何选择敏感性计算暂缓**；"|Δ|<0.2 kcal/mol 即稳健"判据**删除**（无研究目标或误差预算依据）。
- **复用登记**：未来若执行该对照，JOB-018 的 c14 L8 配对结果经兼容性核验后可复用，不默认重算四点。
- 阶段成果（气相基线 / 固定几何电子能 / 开放水相问题 / 所需输入）见 `results/phase1_closeout/phase1_results_summary.md`；应用方向维持"水产养殖污染物去除或鱼类病毒灭活"，具体对象缺失不由脚本补定。

## 边界
历史原始结果与异常证据保留；未修改 DFT 平台或 PySCF 安装环境；全部修改限于项目目录。

**本批停止。新增量化求值数 = 0。**
