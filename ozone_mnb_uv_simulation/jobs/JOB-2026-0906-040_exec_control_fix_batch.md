# JOB-2026-0906-040：测试隔离与起点验收流程修复批

## 范围与授权
- 任务编号 = 039 之后下一个未占用（已核对）。**本批新增 SCF、梯度、优化、稳定性、Hessian、频率均为 0**——只修复项目执行控制。039 原始脚本/账本/日志/结果已建哈希快照保留（`history_snapshot_hashes.json`），错误证据不覆盖。

## 执行状态（全部完成）
1. **零求值审计与计数补正**（`run_artifacts/02_nh3o3_reference/c1_cart_cont039/attempt_audit.json` + `notes/job040_audit_and_corrections.md`）：
   - attempt[0]：测试触发（T2 初版 mock 目标错误）——SCF 启动/完成/梯度均完成（记录在案），当时违规，计入实际成本；
   - attempt[1]：pre_checked——SCF 启动**无法确定**（SIGTERM；无记录），完成否无法确定——按证据标"无法确定"，不重算补证；
   - attempt[2]：正式入口门失败求值——流程缺陷下的未授权求值（起点门未闭合时发生；剩余预算不构成授权）；
   - 汇总：预算 attempt 3；确认启动 SCF 2 + 无法确定 1；完成 2；误触发全部计入成本；
   - **撤回**"本批新增真实求值仅 1 次"不完整口径；
   - 科学结论限定：039 未完成预定对照；起点门误用于其他几何；不否定 038 端点可复现性；越界真实调用一经发现必须停止整个正式流程。
2. **测试隔离修复——不依赖 mock**（新入口 `protocols/c1_cart_exec_v3.py`；导入零副作用；路径写入前守卫；显式依赖注入；"禁止真实调用"哨兵；缺失账本拒绝静默新建；正式执行需显式参数）。
3. **起点门移到优化器启动之前**：来源与单位核验 → 起点完整记录验收 → 门通过 → 启动优化器 → 普通试探点（仅一般检查）；门显式接收起点与参考记录；起点验收落盘后同坐标复用不重复消耗 SCF；恢复需全凭证兼容；不完整 attempt 不猜测。
4. **无 SCF 测试逐项全过**（`test_c1_cart_exec_v3.py`，T1–T10 含子项）：正确起点→模拟优化器才启动；错误起点/单位/配置→优化器调用 0；缓存无凭证不绕门；合格起点复用后第二几何不重触发门；篡改哈希/配置不符/未完成 attempt 拒绝恢复；误传正式路径写入前拒绝；未 mock 优化器仍只调模拟后端（哨兵）；预算耗尽/后处理异常/持久化异常硬停；条件复核同完整保存机制；测试前后正式目录哈希不变。

## 下一批执行流程草案（待批）
```
python protocols/c1_cart_exec_v3.py --execute --backend real --init-new-ledger
```
流程：来源核验（034 末步）→ 单位链 → 起点验收 1 次求值（|ΔE|≤1e-8、梯度标量 ≤1e-7）→ 门过 → L-BFGS-B（gtol=1e-6/ftol=1e-13/maxcor=10）优化 attempt ≤20 → 任一点 max|g|≤1e-5 受控停止 → 独立复核 1 次（完整保存机制）→ 通过则登记"驻点候选，稳定性与频率待查"。预算：总 attempt ≤21（起点验收 1 + 优化 20）；失败计入。

## 边界
**本批新增真实量化计算：0。** 039 原始证据保留；未修改 DFT 平台、依赖或 PySCF 安装。

**本批停止。** 等待下一批授权。
