# JOB-2026-0906-007：SMD 同配置验证修复与未收敛轨迹诊断批

**日期**：2026-09-07　**状态**：完成（A/B/C 全部通过；C 为离线诊断，方案仅提出未执行）
**报告**：`results/01_water_matrices/pure_water_o3_h2o/smd_config_audit/smd_configuration_audit_report.md`
**汇总**：同目录 `smd_configuration_audit_summary.json`

## 阶段 A：配置错误复现与修复 — 通过
- **配置错误确认**：006 阶段 B 生产分支未向 `make_mf_d2_gr` 传 grid_level → 实际 **L7**（指纹记录 7）；原生基准显式 **L8**；阶段 C 显式 L8。
- 影响范围：scanner 复现与同密度 D2-once（L7-vs-L7）保留换标签；gr 活跃性（L8-vs-L8）保留；"同配置独立基准通过"与"L8 五点验证完成"**撤回**。
- 脚本修复：双侧显式 L8；计算前逐项配置断言（指纹读实际对象，DFT 网格与溶剂表面网格分列）；**无 SCF 配置回归 5/5**（单侧 L7 计算前失败）。未改全局默认值。

## 阶段 B：同配置独立验证 — 通过（L8 双侧，门槛未放宽）
- 配置一致 ✓；scanner vs 独立对象 ✓（~10⁻¹³–10⁻¹⁰）；**原生 SMD+显式色散 vs 生产：机器精度**（006 的 3–4.6×10⁻⁵ 残差证实为 L7/L8 跨网格差异，同配置降至 ~10⁻¹²）；同密度 D2-once ✓（~10⁻¹⁴/10⁻¹⁵）；A→B→A ✓；grid_response 执行（路径+配置核查）✓；斜率/曲率一致 ✓。无 missing 项。

## 终点复核 — 两终点 max|g| 记录精确成立
c14 2.3867×10⁻³、c06 2.5803×10⁻⁵（holds=True；D2-once 机器精度）。

## 阶段 C：轨迹诊断（离线）
- **c14_plus**：前 26 步单调弛豫至 g=8.34×10⁻⁵ → **step 30 大步长结构重排**（位移 0.055–0.079 Å）梯度跳回 10⁻³ → 弛豫继续至 60 步截断（最低能量=末步）。**方案**：从 step-60 几何重启 + trust 0.3→0.1，cap 60。
- **c06_plus**：**优化器正常内坐标收敛停止**（非异常退出；判据数值无日志记 missing）；36–40 步梯度平台 2.55–2.71×10⁻⁵、位移≈0。残差性质待判。**方案**：先导数诊断（最大梯度分量有限差分，L8 已验证路径）；真实力 → step-40 几何继续 cap 30；噪声底 → 用户阈值讨论（不静默接受）。

## 报告与包装说明补正（已落实）
- 撤回"原生基准残差=网格响应/跨路径噪声"归因（实为跨网格差异，已实测）。
- undo_solvent：实测仅断言失败；"双重计入"为推测从未观测（notes 已改写）。
- CDS："解析"仅指 Python 层成对调用 C 例程，内部算法未核验。
- 006 报告/JSON 加修订块；原始文件未动；新批数据另存 `smd_config_audit/`。

## 边界
未追加优化、未放宽阈值、未算频率、未新增几何扫描；平台与 PySCF 未动（本批发现为项目配置遗漏，不涉及平台修复）。

## 下一步
验证已闭合 → 下一批安排有限步数水相优化（按上述方案）→ 驻点通过后计算水相频率。本批停止。
