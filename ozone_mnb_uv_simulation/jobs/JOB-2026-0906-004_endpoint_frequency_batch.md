# JOB-2026-0906-004：新端点完整内部频率与软模可靠性验收批

**日期**：2026-09-06　**状态**：完成
**报告**：`results/01_water_matrices/pure_water_o3_h2o/endpoint_frequency/endpoint_frequency_report.md`
**汇总**：同目录 `endpoint_frequency_summary.json`

## 执行内容

1. **端点固定**：从 `phaseC_summary_attempt2.json` 读取正式端点（源哈希已记录）；索引核实（臭氧中心 O=0、端氧 [1,2]、水 H [4,5]）；端点独立重算 L7 E + 完整响应梯度（6×3 已存），两候选 max|g| = 2.641e-6 / 2.142e-6，门槛通过。
2. **上一批补正**（落实于 003 报告/JSON）：逐步完整梯度缺项标注（不虚构不重跑）；撤回"正式配准去重"（rmsd_dedup 仅质心平移）；方法命名统一（ωB97X-D 非经验色散 DFT 部分 xc='wb97xd' + 项目经验色散全导数）；attempt 2 属追加尝试（步数 6/46、13/25），不再追加。
3. **正式去重**：Kabsch（O₃ 框架锚定）+ 等价置换 {1↔2, 4↔5}；c14↔c06 RMSD = 3.745 Å → 不同构型均保留；各端点 vs 自身起点 0.07 Å。产物 `formal_dedup.json`（含最优映射）。
4. **差分 Hessian**：144 点（18 方向 × ± × h=0.002/0.004 Bohr × 2 候选），grad_factory 路径、逐点指纹、坐标核验 <1e-10 Bohr、全部 SCF 收敛、0 坏点；未对称化 Hessian 已存，反对称残差 2.2–9.4×10⁻⁶ 后才对称化。
5. **频率分析**：秩 6 外部子空间校验、U 18×12、UᵀH_mwU 12×12 全谱；**两候选 12/12 内部模式在两步长下全部为正**（最低：c14 42.1 cm⁻¹、c06 32.5 cm⁻¹）；与本机 PySCF harmonic_analysis 交叉核对（h=0.002 差 ~1e-11 cm⁻¹ 机器精度）；两步长按重叠匹配（≥0.992），max|Δω| 0.08/0.77 cm⁻¹。修复一处脚本 bug（h_mw 双重转置破坏合同变换符号保持——诊断记录于日志）。
6. **软模核查**：无负模/翻转模 → 每候选仅最低模 1 方向（<2 上限）；±0.002/0.004 Å × L7/L8 = 32 点；斜率校验残差 ≤1e-9；L7 曲率与 Hessian 投影一致；**L7/L8 符号全正**。c06 曲率网格鲁棒（<1%）；c14 最低模量级 L8 敏感（~3×）且 **c14 的 L8 中心 max|g|=1.475e-5 略超 1e-5**（已记录；L7 为验收网格不受影响）。

## 判定

两候选 = **在本批方法与数值检查范围内支持的局部极小值**（非全局最低）。无鞍点候选、无待定项。

## 边界

未重优化、未追踪负模、未做水相/热化学/结合自由能/高层级/反应路径；平台文件与 PySCF 未动；本批唯一 bug 为项目脚本自身（已修复并在日志记录根因）。

## 下一步（由用户决定）

水合结构筛选保留策略；可选的 c14 最低模网格收敛专项；恢复水相与高层级对照；反应路径研究。


---

## 修订（JOB-2026-0906-005 阶段 A，2026-09-06）

- step3 脚本修复：每步长用**自身** Hsym 构造 PySCF 布局交叉核对 + 同矩阵往返断言 + 回归测试（12/12 通过；T3 证明误传 h=0.002 矩阵时测试大声失败，ratio=1.00000000 复现 004 bug）。撤回"h=0.004 差异=独立对角化舍入"解释；同矩阵交叉核对 h=0.004 = 4.5e-11/1.1e-10 cm⁻¹（机器精度）。产物 `frequency_analysis_revised.json`（取代 004 `frequency_analysis.json` 交叉核对字段）。
- 矩阵比较三分离：Cartesian 差异 / 同 U 基底质量加权内矩阵差异 / 模式匹配；反对称残差结合步长趋势与内部尺度呈现，不冒充严格误差上界。
- 实际软模任务量 = **16 个唯一位移点**（每候选 8）+ 2 个 L8 中心；32 为授权上限非执行量。
- 结论修订：c14_plus = "L7 局部极小值证据支持，网格敏感性**待完成**"（待定项保留）；c06 鲁棒性仅覆盖已查最低方向；L7/L8 曲率比由数据给出（c14 kE 4.03/3.69、kg 3.91；c06 0.995/0.994/0.994）。
