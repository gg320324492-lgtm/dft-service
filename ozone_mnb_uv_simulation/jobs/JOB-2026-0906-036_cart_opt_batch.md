# JOB-2026-0906-036：完整求值持久化修复与有限笛卡尔优化对照批

## 范围与授权
- 任务编号 = 035 之后下一个未占用（已核对）。授权：持久化修复 + 无 SCF 回归通过后直接执行；笛卡尔优化 ≤20 次真实求值（含初始点与全部线搜索点）；达 max|g|≤1e-5 才复核 ≤1；总上限 21。本批不运行稳定性/Hessian/频率。

## 执行状态（全部完成）
1. **JOB-035 补正**（`notes/job036_job035_corrections_2026-09-08.md`）：①"完整梯度复现通过"→"能量和已保存梯度标量复现；完整向量复现无法核验"；②删"逐位复现"及过强表述；③删"强非谐性"（h=0.002 观测与局部二次近似相符：前向斜率≈中心导数+曲率×h/2=−6.31e-4 ✓）；④删"瓶颈已确定为 pyberny"（下一步是策略对照）；⑤如实登记重复中心/缺失位移点（总数≤5≠完成原定组成；梯度向量不可从标量恢复）。
2. **持久化修复（回归 5/5）**：求值后立即原子保存完整记录（坐标/元素序/单位/能量+D2 项/N×3 梯度/配置/SCF 状态/attempt 号）；安全落盘后才标 done；后处理异常记录仍可读（T-A1）；done 在安全落盘后标记（T-A2）；重启核验复用不重算（T-A3）；配置变化不复用（T-B）；总上限求值前拒绝（T-C）。测试注入点：求值成功后人为触发后处理异常。
3. **笛卡尔对照 20/20 求值**（SciPy L-BFGS-B；21 Bohr Cartesian 变量；无约束/不投影/不配准；gtol=1e-6、ftol=2.22e-9、maxcor=10 运行前记录；fun 单次调用返回能量+梯度；硬限制=求值前持久化账本）：
   - **未改善**：全部保存点最佳 max|g|=2.21×10⁻³（差于 034 起点 3.02×10⁻⁴）；最低 E=−282.001257（高于起点 −282.001717）；L-BFGS-B 单位初始 Hessian 大步导致先升后游走；
   - 无保存点达 max|g|≤1e-5 → **复核 0 次**；预算硬停（scipy 未返回）；
   - **判定：登记未收敛 + 有限对照未完成**（不外推）；未改善不证明 pyberny 根源错误，也不自动宣布数值地板或放宽标准。

## 证据现状（供路线决定）
- pyberny 内坐标：50 次求值（033 30 + 034 20，两次独立初始化）→ max|g| ~3×10⁻⁴；
- L-BFGS-B 笛卡尔：20 次求值 → 2.2×10⁻³（未回到起点水平）；
- 残余梯度来源仍未定位；两套策略均未达严格判据。

## 交付
`results/phase2_preparation/c1_cart_opt_report.md`、`run_artifacts/02_nh3o3_reference/c1_cart_opt/`（eval_records.json 完整逐次记录、budget_cart.json、cart_opt_results.json、cart_run.log）、驱动 `protocols/c1_cart_opt.py`、持久化回归 `test_c1_cart_opt.py`（5/5）、JOB-035 补正记录、任务记录（本文件）。

**本批停止。** 优化求值 20/20；复核 0（触发未满足）；模拟测试 0（持久化回归无 SCF）。
