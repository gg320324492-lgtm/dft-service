# JOB-2026-0906-035：NH3···O3 未收敛端点梯度一致性诊断批

## 范围与授权
- 任务编号 = 034 之后下一个未占用（已核对）。总上限 **5 次能量—梯度求值**（中心 1 + 位移 4；失败计入；历史额度不结转）。不计算 Hessian/频率/稳定性/CP/TS/IRC/高层级/水相。

## 执行状态（完成；预算 5/5 用尽）
1. **离线分析与补正**：撤回"梯度地板稳定出现""极平坦势面扩散"归因 → 实际观察（末段能量变化小、梯度振荡、几何仍变化）；**记录缺口登记**：033/034 轨迹只存 grad_max/grad_rms 无完整梯度向量 → 分解移至独立中心梯度；交叉核验：两批收敛参数/阈值/扫描路径一致、无 kernel 补丁、无配置或绑定错误（grad_max-步长相关 0.853 仅描述）。
2. **中心复现：通过（两次独立对象）**——dE=7.4×10⁻¹³ Eh、dgrad_max=8.9×10⁻¹¹、dgrad_rms=3.4×10⁻¹²（门限 1e-8/1e-7；标量门限适配已登记）。端点可精确复现，无调用路径/SCF 分支/配置差异。
3. **固定方向核查**：q=−g/‖g‖₂（21 分量，不投影不配准）：
   - **h=0.001 完整 ± 对**：g₀·q = −grad_rms×√21 = **−0.00113900** vs 中心差分斜率 **−0.00113898** Eh/Bohr → **相对差 1.5×10⁻⁵，一致**；能量曲率 +0.5075 Eh/Bohr²（局部凸，仅交叉检查）；
   - **h=0.002 仅 + 侧**（− 侧预算耗尽未评估）：单侧前向斜率 −6.31×10⁻⁴ ≈ 中心斜率一半 → 端点附近 PES 快速变平（强非谐）——与平坦势盆小步游走自洽；
   - 梯度差分投影曲率不可算（位移点完整梯度未持久化——持久化缺陷已登记）。
4. **判读**：复现通过 + h=0.001 导数一致 → **该端点、该方向的导数路径有支持**；未收敛是**优化器收敛行为问题**（严格内坐标判据在平坦非谐势盆无法满足），非导数路径或配置问题；一个方向不能证明全部导数正确；不宣称严格误差界；沿 −g 下降不是独立极小值证据。

## 求值计数
实际求值 **5/5**（崩溃运行中心 1 次按失败尝试计入——其完整向量随进程丢失，账本存标量；重跑中心 1 次；位移 3 次；minus_h0.002 未评估）。

## 下一步建议（仅一条，待批，不执行）
**更换优化器收敛策略**：从 034 末步几何以笛卡尔优化器（如 scipy L-BFGS-B）继续。**解决的问题**：诊断证明导数路径无问题，瓶颈是 pyberny 内坐标判据（1e-6）在平坦非谐势盆无法满足；笛卡尔优化器以 Cartesian 梯度范数判收敛，绕开内坐标判据。

## 交付
`results/phase2_preparation/grad_diag_report.md`、`run_artifacts/02_nh3o3_reference/grad_diag/`（diag_results.json、offline_gradient_decomposition.json、budget_diag.json、diag_run.log）、驱动 `protocols/{grad_diag_offline, grad_diag_run}.py`、任务记录（本文件）。

**本批停止。**
