# JOB-2026-0906-030：扫描执行入口单位、格式与预算闭合批

## 范围与授权
- 任务编号 = 029 之后下一个未占用（已核对）。**新增量化求值数 = 0**（仅校验模式与模拟后端测试，无电子结构计算）。

## 执行状态（全部完成）
| 项 | 交付 |
|---|---|
| 1. JOB-029 补正 | `notes/job030_corrections_2026-09-08.md`：029 的 11 项测试通过事实保留，但未覆盖——①dE/ds 除法应为乘法；②ds/dd 稀疏割线应改局部公式；③单位测试自证（系数设 1）；④XYZ 缺注释行+读取器自证；⑤预算总数按 category 查找恒 0；⑥缺端到端。执行条件不再标记为全部具备 |
| 2. 单位与局部导数 | dE/ds[Å] = g_proj[Eh/Bohr] × 1.8897（**乘法**）；dd/ds = ½Σ[(N−O_j)·û/|N−O_j|]（逐几何局部，不对称保留）；**真实 Bohr/Å 独立核对**：Bohr 定义能量函数 + Å 坐标独立差分，偏差 <1e-6（除法实现偏差 >50% 可检出） |
| 3. 标准 XYZ | `inputs/nh3o3_phase2/c1_scan_v2_std/c1_scan_std_d*.xyz`（注释行标准格式）；**物理坐标与旧文件一致（<1e-12）**；读取器检查数/元素序/有限性/恰 7 原子 + 独立通用读取器交叉验证 + 8 原子拒绝测试；旧文件保留，`scan_geometry_manifest_std.json` 记录新哈希与版本关系 |
| 4. 预算闭合 | 总次数 = 全部实际 attempt（不分类别）；测试 6 项：总上限 2→第三次求值前拒绝；多类别共用总预算；重启保留；异常保留 attempt+错误；缺账本拒绝静默新建——全过 |
| 5. 真实执行入口 | `protocols/c1_scan_exec_v2.py` 完整流程：读清单（双读取器）→全几何预检→逐点新对象→配置检查→求值前预算→SCF→**收敛检查（不收敛不继续梯度）**→梯度→有限性→完整记录（全精度梯度/实际坐标/配置/能量分项 e_total/e_D2/e_DFT/dE-ds-dd 双口径/ΔE_nonCP）→原始保存；**任何失败保存现场 raise（从不自动进入下一点）**；默认校验模式，`--execute` 才求值，`--init-new-ledger` 显式初始化 |
| 6. 端到端模拟测试 | 走真实入口流程仅换后端：校验模式 6 点 0 求值 ✓；执行模式 d=3.2 注入 SCF 失败 → 3 点后硬停、3.6/4.2 未评估 ✓；逐点完整记录 ✓；重启（修复后端）→ 3.2 重求值、3.6 完成、**4.2 因总预算耗尽诚实拒绝**（attempts=6=cap，失败计入）✓ |

## 下一批实际执行命令与固定预算（待批）
```
python protocols/c1_scan_exec_v2.py --execute --init-new-ledger
```
- 固定预算：总 6 次、scan 分项 6 次（每点 1 次 SCF+完整梯度；无 Hessian/频率/CP/TS/高层级）；失败计入、重启不重置、账本 `budget_scan_exec_v3.json`（--init-new-ledger 显式创建）；
- 结果：`inputs/nh3o3_phase2/c1_scan_v2_std/exec_results_v3.json`；分析框架沿用 028。

## 边界
本批新增量化求值数 = **0**；六点物理坐标不变；旧文件/历史结果保留；未修改 DFT 平台、PySCF 安装或桌面文献原件。

**本批停止。** 待总指挥审核后执行六次求值。
