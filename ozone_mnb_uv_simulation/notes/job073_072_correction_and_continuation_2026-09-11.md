# JOB-073 阶段A：JOB-072 汇总补正与续算授权记录（2026-09-11）

机器可读记录：`run_artifacts/02_nh3o3_reference/endpoint_cont073/audit073_results.json`（含逐项审计与补正字段）。本记录不修改任何原始JSON/日志/旧报告正文；补正通过横幅＋本文件＋机器记录实现。

## 1. 表述补正

1. **HOO·（P1片段）**：JOB-072端点表中"ACCEPTED (project method)"易被读作极小值地位。补正为：**"优化、独立复核和内部轨道稳定性通过的驻点候选，振动曲率未核查"**——stable_i=True来自`return_status=True`的明确返回值（JOB-070 stability_hoo_record.json），但该几何**未经频率/曲率检查，不得称局部极小值**。JOB-073阶段D将对其做条件式频率核查，结果出来前按"驻点候选"表述。
2. **HNO（P4产物）**：保留"在本方法和检查范围内支持局部极小值"——依据是JOB-069已完成TR投影频率检查且零负模。此表述不变。
3. **H₂O₂、H₂NO·**：维持"未验收"（JOB-068/070预算耗尽未达1×10⁻⁵门限）。本批从各自末次实际求值几何续算，不使用模板坐标、不猜测起点。
4. **统计口径**：窗口统计改为"**53 attempts、0次SCF失败、JOB-068有1次零SCF流程异常**"（原"0异常"不准确——JOB-068首次运行稳定性施加于未经kernel的mf对象崩溃一次，零SCF消耗，现场已归档并于068续算修正）。旧条目不改正文，以本条为准。
5. **能量拼接状态**：P4完整能量与P1片段能量**仍未拼接**（各自需两个端点全部通过规定验收）。

## 2. 末次求值审计结论（零求值）

| 分支 | 来源 | 末次记录 | E (Eh) | max\|g\| | 溯源核验 |
|---|---|---|---|---|---|
| H₂O₂ | JOB-068账本自动定位 | h2o2_15 | −151.5646927211 | 2.048×10⁻² | 坐标哈希=accepted_iterate原始字节，精确匹配；config逐键=父批运行时配置；chk存在（b68cd568b105357c） |
| H₂NO· | JOB-070账本自动定位 | h2no_15 | −131.1043110771 | 2.082×10⁻² | 同上；⟨S²⟩=0.75437（Δ=+0.00437）；chk存在（c743c88a4e3f866b） |

元素序（O,O,H,H / N,O,H,H）取自父批manifest；单位（坐标Å、能量Eh、梯度Eh/Bohr）登记；起点=末次实际坐标（Å）÷0.52917721092转Bohr，哈希链闭合，**无任何硬编码或模板替代**。

注：哈希核验以accepted_iterate原始Bohr数组为准；"Å→Bohr往返哈希"因除法1 ulp效应仅作信息性记录（audit JSON内已注明）。

## 3. 续算授权与边界（用户2026-09-11指令）

- 每分支独立账本/结果目录/checkpoint；H₂O₂（RKS singlet）23上限=起点验收1＋续优化20＋复核1＋稳定性1；H₂NO·（UKS doublet, spin=1）同构23上限；互不借用；
- 门限不变：未投影Cartesian max|g|≤1×10⁻⁵；达标立即复核（1）→稳定（1，`return_status=True`四元组）；stable_i≠极小值；
- 条件式频率核查（HOO·优先，续算通过者次之）：每端点≤1次新对象端点确认＋1次DFT解析Hessian＋1次显式D2 Hessian；D2有限差分梯度调用单列不入SCF账本；保留全部负模；stable_i与"零负模"分开报告；仅零负模可登记局部极小值支持；
- 异常→保存现场停止该分支，不静默重试、不改阈值；两分支均未达标→如实停止、不外推剩余步数；
- 不猜测TS、不启动TS/IRC/CP/BSSE/SMD/UV/界面模型；不依赖作者是否回复；结果继续标注项目方法模型、非S13复现。

## 4. 新文件位置约束

本批所有新文件仅写入 `jobs/`、`notes/`、`results/`、`run_artifacts/`（脚本位于 `run_artifacts/02_nh3o3_reference/endpoint_cont073/scripts/`）；未修改 `dft_service/`、`scripts/`、入口脚本、依赖、测试、PySCF安装或平台配置；桌面文献原件只读。
