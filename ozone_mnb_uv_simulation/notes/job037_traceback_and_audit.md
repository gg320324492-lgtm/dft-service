# JOB-037 溯源与记录审计（零求值，2026-09-08）

## 总指挥四项发现的逐项核实（全部确认）

### 1. 036 实际读入的是 033 末步（非 034 末步）——确认
| 来源 | 坐标哈希（前16） | 与 cont_start.xyz 最大偏差 |
|---|---|---|
| **033 step 30**（c1_full_opt） | b317f5a4f7df7f2c | **4.9×10⁻¹³ Å（一致）** |
| 034 step 20（c1_full_opt_cont） | 5c9554e0c8b2125e | 1.06×10⁻¹ Å（不同） |
| cont_start.xyz | 650e5246519bcb69 | — |

**根因**：034 批的续算输入提取脚本在 c1_full_opt_cont 结果文件尚不存在时运行，其断言 `n_opt_steps == 30` 只能被 033 的文件满足——**来源核验形同虚设**。JOB-036 的"起点核验通过"表述**撤回**。

### 2. Å 数值直传 unit='Bohr'——确认（以实际分子对象量化）
- 用 033 末步 Å 数值以 `unit='Bohr'` 构建实际分子对象：N—中心氧距离 = **1.6770 Å**（预期 3.1691 Å），**比值 0.529177**（= 0.52918 Å/Bohr 的倒数换算因子）；最大原子间距比值 0.529177——**整体结构缩小到 0.529 倍**，非变量名检查可发现。

### 3. 首个真实求值对照——确认
- cart_run.log：`[cart] eval 1 E=-275.008373537 max|g|=8.375e+00`（缩放结构的爆炸梯度）；
- **撤回**："正确起点下优化策略无效"与"初始 Hessian 大步导致失败"的归因——20 次求值全部发生在**缩放的错误结构**上，与预定起点的优化器行为无关。

### 4. 034 末步参考值（原始记录）
- E = **−282.00171709790953 Eh**；max|g| = **7.5951845×10⁻⁴**；grad_rms = **2.4855024×10⁻⁴** Eh/Bohr。
- 033 末步（036 实际误用）：E = −282.001645831、max|g| = 3.0199×10⁻⁴。两几何最大分量差 0.106 Å——**不同几何**。

### 5. eval_records.json 审计（22 条 = 20 真实 + 2 测试）
- **20 条真实**（attempt 0–19，E ~ −282 尺度，对应正式账本 budget_cart.json 的 20 次 done）；
- **2 条测试混入**：
  - attempt=0、key=86dfa97e078d27c2、E=−0.99375（036 首轮测试的 hook_boom 记录——当时 make_objective 尚无 records_path 参数）；
  - attempt=1、key=3d17e96c0a4b9723、E=+0.40625（**T-C 测试漏传 records_path**，写入正式文件）；
- **账本关联**：budget_cart.json 的 20 次 attempt 全部对应真实记录（测试记录不与正式账本 attempt 关联——测试用了独立账本但误写正式结果文件）；
- **分类规则**（非能量筛选）：真实 = E 在 −282 尺度且 e_d2 有限；测试 = 谐振假后端特征值或 E 标度不符；
- 原文件保留不动；分类索引见 `run_artifacts/02_nh3o3_reference/c1_cart_opt/eval_records_classification.json`。

### 6. 036 的 20 次真实求值
计入历史成本，**不作为预定起点（034 末步）的优化器对照证据**（对象为缩放错误结构）。

## 036 结论撤回清单
1. "起点核验通过（JOB-034 step 20）"——**撤回**（实为 033 step 30）；
2. "笛卡尔策略在该起点未改善"——**撤回**（对象为缩放错误结构，与预定起点无关）；
3. "L-BFGS-B 单位初始 Hessian 大步导致先升后游走"——**撤回**（同上）；
4. "不证明 pyberny 有根源性错误、不宣布数值地板"——**保留**（本来就不成立）；
5. "证据现状：pyberny 50 次→3e-4；L-BFGS-B 20 次→2.2e-3"——**修订**：L-BFGS-B 的 20 次不适用于预定起点对照，仅计入历史成本。

## 034 末步参考值同表（供 037 复现验收）
E = −282.00171709790953 Eh；max|g| = 7.5951845×10⁻⁴；grad_rms = 2.4855024×10⁻⁴（来源：c1_full_opt_cont/exec_results_formal.json trajectory[19]）。
