# JOB-053 中断审阅与续算授权

已只读核对053中心记录、052端点、预算及两个执行入口。endpoint_eval在返回(rec,mf,mol)前已保存完整记录；053调用者错误地按字典使用元组，导致求值完成后异常。053原账本1条centre error，位移0。原异常及账本保留，不直接改写为无异常done。

中心记录与052的离线比较：dE=1.7053e-13 Eh，完整梯度最大差1.2017e-12 Eh/Bohr，实际坐标差0，配置一致、SCF收敛，gmax=8.202783992729223e-6。可在完成来源/哈希/有限性核查并保存正式恢复凭证后复用，无需再做中心SCF。

授权JOB-053 resume-01：零求值修复验证及中心门补登记通过后，只执行原定四个位移，各1次SCF+完整梯度，总新增上限4。原批已完成中心1次继续计入历史成本，累计最多5次；原流程异常1次单列，不能说整批0失败。恢复记录、账本、日志另存独立子目录，原始error不覆盖，以追加恢复凭证关联已成功持久化的计算。

测试必须覆盖真实包装函数的三元组接口（底层注入模拟对象，不触发真实后端）、求值完成后异常但文件仍在、仅验收复用已保存中心而不重复求值、门失败不进入位移、剩余四次预算及硬停。

任何新异常或来源不符立即停止，不重做中心、不自动重试、不挪用预算；其余方法、方向、几何、分析公式和边界沿用053原指令。本次总指挥仅离线审阅，未运行量化计算。续算指令已下达，尚未收到结果。
