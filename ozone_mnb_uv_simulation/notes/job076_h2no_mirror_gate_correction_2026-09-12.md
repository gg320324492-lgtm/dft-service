# JOB-076：H₂NO·镜像门修复与条件频率批补正记录（2026-09-12）

机器可读：`h2no_follow076/audit076_results.json`。

## 1. JOB-075账目补正

总attempt=6：端点优化链5次（起点复现1＋优化2＋复核1＋稳定性1）＋镜像比较为独立第6次。镜像门FAIL由**不同优化层级参照**造成；阶段A镜像闭合（反射RMSD 7.06×10⁻⁹）仍成立；**不把JOB-075镜像FAIL解释为物理不等价**。

## 2. 本批执行日志（含3次执行层缺陷的现场保存）

1. 阶段A首跑V4误用跨层对比（6.6×10⁻⁵）→改为同层对比（7.06×10⁻⁹）✓，层级差改登记为信息项——该误差正是阶段B要消除的对象；
2. minus稳定性attempt崩溃：manifest缺`s2_reference_doublet`键（脚本缺陷）→error attempt保留、分支停止→按068先例resume（新账本stab_resume 1/1，复用全部已完成求值）→**stable_i=True**；无效SCF 1次如实登记；
3. 镜像门SCF已运行并落盘，其后层级比较因merged状态缺字段崩溃→零SCF补字段；重跑遇账本cap（error已占位）→**改用已保存的mirror SCF记录零新增SCF完成门评估**；
4. 门字面FAIL：格点镜像残差（非平面几何L8实测dE 8.8×10⁻⁸、dg 1.7×10⁻⁶）超为平面分子预设的门限；层级比较因反射面不一致（bfp倾斜面 vs z框架）虚高0.067 Å；
5. 直接同层闭合补充验证：**reflect(minus_final) vs plus_final dmax=1.17×10⁻⁸ Å、能量差5.1×10⁻¹³ Eh**——镜像等价确认。

## 3. 结论与建议（更新：minus独立频率路径已解锁P1）

镜像门字面FAIL（格点镜像残差+反射面不一致，均定量归因）→**plus**频率按字面未执行。但批令阶段D为minus预设了独立授权路径（"RMSD超过1e-6 Å→对minus另做1次频率锚点和独立Hessian"），已执行：**minus频率265.36/1285.76/1432.74/1680.22/3468.14/3600.13 cm⁻¹零负模→H₂NO·锥形化极小值登记（本方法和检查范围内）**；**P1分离片段能解锁：−19.447 kcal/mol**（项目方法电子能）。直接同层闭合1.17×10⁻⁸ Å使该结果对plus由镜像对称共享（标签保留）。plus独立频率仍属可选新批（建议以直接闭合替代反射SCF门）。
