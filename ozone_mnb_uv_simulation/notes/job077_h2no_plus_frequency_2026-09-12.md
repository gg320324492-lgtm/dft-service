# JOB-077：H₂NO· plus镜像端点独立频率确认（2026-09-12）

授权：用户JOB-077批令。目录：`run_artifacts/02_nh3o3_reference/h2no_plus_freq077/`。审计：`audit077_results.json`；比较表：`mirror_comparison077.json`。

## 1. 阶段A：零求值起点核验（九项全过）

起点=**JOB-075 plus最终复核记录**（meeting_tag=opt_02，门控PASS：dE≤1e-8/dgrad≤1e-7/dcoords≤1e-9）；坐标哈希自洽、链源为plus（非镜像导出坐标）；**禁用清单**（074 plus/minus末点、镜像反射坐标、未求值坐标）全部核验未使用；076 minus频率参照就绪（6模、零负模）。stable_i**沿用JOB-075记录**（return_status=True，来源注明），本批不重算。

## 2. 阶段B：plus独立频率

锚点PASS（dE=1.7×10⁻¹³量级，新对象）→解析DFT Hessian（grid_response=True）＋显式D2 Hessian（FD 24次单列）→合成核验≤1e-10→TR投影→**全部6个内频**（负号保留）。矩阵五件套落盘：未对称化/对称化/DFT部分/D2部分/模式向量。

## 3. 阶段C：正负镜像频率比较（零求值）

比较表（`mirror_comparison077.json`）：plus vs minus逐模式绝对差、正负模数、6×6模式重叠矩阵（z框架反射下的镜像映射）及最优匹配重叠、⟨S²⟩与自旋污染对照。判定规则：两者均零负模→**分别**登记局部极小值（独立镜像标签，永不合并）；若plus出现负模→不覆盖minus、进入离线诊断。频率近似相同只作镜像对称性证据，不称重复结构。

## 4. 阶段D：P1片段能量

若plus零负模：**保持JOB-076的−19.447 kcal/mol**（数值不变——能量由已验收端点能量构成，本批零新增SCF进能量表）；表述更新为"两个H₂NO·镜像端点均完成频率级核查"；口径不变：气相、项目方法电子能、无CP/ZPE/热修正、非P1整体反应能、非S13复现。

## 5. 边界

不重新优化、不重建几何、不改变自旋方案；不把镜像推断冒充独立结果（plus频率为本批独立Hessian计算）；负频保留负号；异常即停；平台/依赖/文献原件只读；073–076原始文件不覆盖。
