# 未来约24小时自动计算计划

日期：2026-09-11。目标是在用户暂时无法反馈期间，连续推进P4/P1(2)反应端点，同时保持每批独立预算、完整记录和严格的停止边界。作者三维坐标获取为并行增强证据，不是项目模型计算的前置条件。

## 顺序

1. JOB-068：HNO和H2O2项目模型产物端点优化。
2. JOB-069：对通过复核的P4产物端点做电子稳定性和谐振频率；必要时计算自定义热化学。
3. JOB-070：HOO·和H2NO·双重态自由基端点优化、复核和稳定性检查。
4. JOB-071：P1(2)整体自旋耦合的零求值准备；只有在P1两个端点均可靠时才允许有限的分离片段自旋对照，不猜测TS。
5. JOB-072：零求值汇总、数据表和汇报用SVG图；不制作最终PPT，不把不同方法混表。

## 总边界

计划墙钟上限20小时，保留4小时缓冲。总SCF+完整梯度attempt上限约72（按各批分项账本执行）；失败、超时和启动后终止均计入相应批次。任何平台、依赖、桌面文献原件和历史结果只读。

## 继续条件

某批正常完成或明确not_converged（现场完整保存）后才进入下一独立批；发生路径完整性、配置不符、持久化失败或真实后端异常时停止该批并记录，不自动修复重启。不得为了“跑满24小时”追加未授权求值。

## 执行结果（2026-09-11 补记）

窗口按顺序完成：JOB-068（前序会话，HNO验收/H₂O₂预算耗尽）→JOB-069（仅HNO频率验收：零负模，局部极小支持登记，热化学登记）→JOB-070（HOO·全链路验收⟨S²⟩=0.754；H₂NO·预算耗尽未验收）→JOB-071（自旋耦合零求值准备）→JOB-072（汇总与SVG/CSV数据包）。总账目53 attempt（068=26、069=2、070=25），上限约72，**未追加未授权计算**；0计算失败。两个"第二产物"（H₂O₂、H₂NO·）均预算耗尽未达1e-5门限——其续算属新授权决策；E_products与dE_P1frag均未拼接。TS/IRC未启动；等待作者坐标。
