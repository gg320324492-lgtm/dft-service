# JOB-074：H₂NO·负模方向跟随与真实极小值搜索（2026-09-11）

授权：用户JOB-074批令。目录：`run_artifacts/02_nh3o3_reference/h2no_follow074/`。机器可读审计：`audit074_results.json`。

## 1. 对JOB-073的机器可读补充说明（原文不动）

1. H₂NO·的−193.61 cm⁻¹内禀负模**证明当前几何不是局部极小值**；
2. **只有在IRC验证后**才能讨论该驻点是否为过渡态——本窗口不运行IRC，负模**不得自动称为TS**；
3. H₂NO·当前登记为**"负曲率驻点/一阶鞍点候选"**，不登记为极小值；其能量不可用于P1片段参考。

## 2. 负模向量溯源核验（零求值，全部通过）

向量**只从JOB-073保存的频率记录读取**（norm_mode[0]，PySCF thermo约定），严禁手工重建。核验：

| 项 | 结果 |
|---|---|
| V1 维度 | (6,4,3)，4原子×3 ✓ |
| V2 质量加权单位范数 | ‖q_mw‖=1±1e-8 ✓ |
| V3 TR纯度 | 从**保存的Hessian+质量**重算U，‖Uᵀq_mw‖=1±1e-6——模在内部补空间，非平动/转动污染 ✓ |
| V4 本征对应 | 重算λ₀=−1.418548×10⁻³ Eh/Bohr²/amu = 保存值（≤1e-10）；最低本征向量与模重叠≈1 ✓ |
| V5 频率对应 | sign(λ)√\|λ\|·AU2WN 复现−193.609 cm⁻¹（<1e-3）✓ |
| V6 归一化/符号 | Cartesian方向q_cart=norm_mode[0]/‖·‖（单位范数）；符号规则=首个\|v\|>1e-8分量取正（预登记，翻转已记录）✓ |

约定登记：norm_mode保存的是**质量加权模式对应的Cartesian位移花样**（q_mw/√m）；位移用**单位范数Cartesian方向**构造，0.10 Bohr为真实Cartesian距离。预期曲率（信息性）：q_cart方向curvature=λ₀/\|d_raw\|²，两侧预期对称下降~½·curv·0.01 Eh——仅作参考，实际能量决定。

## 3. 预算与分支结构（SCF+完整梯度上限39）

| 账本 | 类别 | 上限 |
|---|---|---|
| 主账本 budget_center_disp074.json | center 1 + disp 2 | 3 |
| plus/budget_plus074.json | opt 15 + recheck 1 + stab 1 + anchor 1 | 18 |
| minus/budget_minus074.json | 同构 | 18 |

位移：R±=R0±0.10·q_cart（R0=JOB-073复核记录实际坐标；0.10 Bohr固定预登记，不得自动扩大）；位移记录实际坐标/单位/哈希/能量/完整梯度/⟨S²⟩/配置。两分支独立账本+独立checkpoint；两侧都保留分析（不只因能量低留一侧）；一侧异常只停该侧；最终结构以元素序+正规旋转(Kabsch)+RMSD去重，不强行合并。15次未达门限照实停止，不外推。

## 4. 频率与P1条件

频率（每合格分支：anchor 1＋DFT解析Hessian 1＋显式D2经典1，D2 FD单列）：全部频率保留符号；零负模→"在本方法和检查范围内支持局部极小值"；仍有负模→"非极小值/负曲率结构"，不称TS（TS仅IRC后）。P1分离片段能**仅当至少一分支通过复核+stable_i+零负模**时离线计算（与HOO·@070已验收能量求和；无CP/ZPE/热；不称P1整体反应能）。

## 5. 边界

新文件仅入本项目对应目录（脚本在h2no_follow074/scripts/）；dft_service/、scripts/、依赖、入口、测试、PySCF安装、平台配置只读；文献原件只读；JOB-073原始结果/日志/频率矩阵/账本不覆盖；任何SCF/单位/路径/配置/账本/持久化异常→保存现场停对应分支，不静默重试、不放宽门限。stable_i全部来自`return_status=True`布尔返回。作者数据未回复不阻塞本批。
