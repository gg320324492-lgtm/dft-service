# JOB-040 审计与补正记录（2026-09-08）

## 一、JOB-039 逐 attempt 审计（详细证据见 `run_artifacts/02_nh3o3_reference/c1_cart_cont039/attempt_audit.json`）

| attempt | 触发来源 | SCF 启动/完成/梯度 | 当时授权符合性 | 处置 |
|---|---|---|---|---|
| [0] E=−282.00171974160537（与 038 末接受点参考值精确一致） | **测试**（T2 初版 mock 目标错误：patch `so.minimize` 而非模块内 `W.minimize`，真实后端对正式路径执行） | 是/是/是（完整 7×3 已存 eval_records） | **违规**（测试代码读写正式路径） | 计入实际成本 |
| [1] pre_checked | 测试（同一次运行；线搜索试探） | **无法确定**（120 s 工具超时 SIGTERM 杀进程；无记录写入） | **违规**（同一次运行） | 按"无法确定"登记，不重算补证 |
| [2] 门失败试探点（距起点 2.1×10⁻⁴ Å） | **正式入口**（039 续优化） | 是/是/是 | **流程缺陷下的未授权求值**（起点门未闭合；剩余预算不构成授权） | 照实登记 |

## 二、计数补正（撤回口径）

- **撤回** JOB-039"本批新增真实求值仅 1 次"——不完整口径；
- 汇总：预算 attempt **3**；确认启动 SCF **2** + 无法确定 **1**；完成 SCF＋梯度 **2**；不确定 **1**；
- **误触发计算（测试引发）全部计入实际成本**（attempt 0 完整 + attempt 1 不确定）。

## 三、科学结论限定

1. 039 **未完成预定续优化对照**；
2. **起点门被误用于其他几何**（线搜索试探点不适用起点能量/梯度一致性要求）；
3. **不据此否定 038 端点的可复现性**（037 已独立验证）；
4. 隔离违规后的修复与继续执行经过已如实登记；**后续一旦发现越界真实调用，必须停止整个正式执行流程**。

## 四、执行控制修复要点（新入口 `protocols/c1_cart_exec_v3.py`）

1. **显式依赖注入**：测试只能取得模拟后端，无自动回退真实后端；
2. **导入零副作用**：模块导入不建目录、不开账本、不启动计算；
3. **正式执行三重显式要求**：`--execute` + `--backend real` + `--init-new-ledger`；
4. **路径写入前守卫**：解析后路径位于正式目录即拒绝（PathGuardError）；
5. **移除隐式正式默认值**：records/results 路径必传；
6. **"禁止真实调用"哨兵**：未 mock 优化器时仍只调用模拟后端（真实后端不可达）；
7. **缺失账本拒绝静默新建**；正式执行不自动重启。

## 五、起点门移到优化器启动之前

顺序：**来源与单位核验 → 起点完整记录验收（1 次独立求值）→ 门通过 → 启动优化器 → 普通试探点求值（仅一般检查）**。门函数显式接收指定起点与参考记录（不依赖缓存命中顺序）；门未过优化器不启动；验收落盘后同坐标复用不重复消耗 SCF；恢复需全凭证兼容；不完整 attempt 不猜测为完成。
